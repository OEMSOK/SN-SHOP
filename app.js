// LOCALIZATION RESOURCE DICTIONARY
const i18n = {
  km: {
    app_title: "ហាង សុខ ស៊ីណេត",
    nav_dashboard: "ផ្ទាំងគ្រប់គ្រង",
    nav_debtors: "អ្នកជំពាក់",
    nav_debts: "បញ្ជីជំពាក់",
    nav_history: "ប្រវត្តិសង",
    nav_settings: "ការកំណត់",
    search_placeholder: "ស្វែងរកឈ្មោះ ឬលេខទូរសព្ទ...",
    search_debtors_placeholder: "ស្វែងរកតាមឈ្មោះ ឬលេខទូរសព្ទ...",
    search_debts_placeholder: "ស្វែងរកតាមឈ្មោះ មុខទំនិញ...",
    search_history_placeholder: "ស្វែងរកតាមឈ្មោះអ្នកជំពាក់...",
    quick_add: "បន្ថែមការជំពាក់",
    dashboard_title: "របាយការណ៍សង្ខេប",
    dashboard_subtitle: "ទិដ្ឋភាពទូទៅនៃបំណុលអតិថិជន និងការសងប្រាក់",
    stat_total_debt: "ជំពាក់សរុប (Unpaid)",
    stat_total_paid: "សងរួចសរុប (Paid)",
    stat_debtors_count: "ចំនួនអ្នកជំពាក់សរុប",
    stat_active_debtors: "នាក់កំពុងជំពាក់",
    chart_title: "របាយការណ៍បំណុលតាមខែ (គិតជាលុយរៀល)",
    legend_unpaid: "មិនទាន់សង",
    legend_paid: "សងរួច",
    recent_activities: "សកម្មភាពថ្មីៗ",
    view_all: "មើលទាំងអស់",
    no_activities: "មិនទាន់មានសកម្មភាពនៅឡើយទេ",
    debtors_title: "បញ្ជីអ្នកជំពាក់លុយ",
    debtors_subtitle: "គ្រប់គ្រងព័ត៌មានអតិថិជន និងមើលព័ត៌មានលម្អិតបំណុល",
    add_debtor_btn: "បន្ថែមអ្នកជំពាក់ថ្មី",
    debts_title: "បញ្ជីការជំពាក់ប្រាក់សរុប",
    debts_subtitle: "បញ្ជីលម្អិតនៃអីវ៉ាន់ដែលជំពាក់ និងការទូទាត់ប្រាក់",
    add_debt_btn: "កត់ត្រាការជំពាក់ថ្មី",
    filter_all: "ទាំងអស់",
    filter_unpaid: "មិនទាន់សង",
    filter_partial: "សងខ្លះ",
    filter_paid: "សងរួច",
    col_debtor: "អ្នកជំពាក់",
    col_description: "ឈ្មោះទំនិញ / ពណ៌នា",
    col_amount: "ចំនួនទឹកប្រាក់",
    col_remaining: "នៅខ្វះ",
    col_date: "កាលបរិច្ឆេទ",
    col_due_date: "ថ្ងៃសង",
    col_status: "ស្ថានភាព",
    col_actions: "សកម្មភាព",
    col_amount_paid: "ចំនួនប្រាក់បានសង",
    col_date_paid: "ថ្ងៃសងលុយ",
    col_payment_method: "វិធីសាស្ត្រទូទាត់",
    col_notes: "ពេលវេលា",
    col_debt_item: "ជំពាក់លើមុខទំនិញ",
    no_debts_found: "រកមិនឃើញទិន្នន័យជំពាក់លុយទេ",
    no_payments_found: "មិនទាន់មានការសងប្រាក់នៅឡើយទេ",
    settings_title: "ការកំណត់ប្រព័ន្ធ",
    settings_subtitle: "កំណត់ចំណូលចិត្ត និងគ្រប់គ្រងទិន្នន័យសុវត្ថិភាពរបស់អ្នក",
    settings_lang_title: "ភាសាប្រព័ន្ធ (Language)",
    settings_theme_title: "របៀបបង្ហាញ (Theme Mode)",
    theme_light: "ពន្លឺ",
    theme_dark: "ងងឹត",
    settings_data_title: "ការគ្រប់គ្រង និងថែទាំទិន្នន័យ (Database)",
    db_warning_note: "ទិន្នន័យត្រូវបានរក្សាទុកតែលើកុំព្យូទ័រ/ទូរសព្ទរបស់អ្នកប៉ុណ្ណោះ។ ដើម្បីការពារការបាត់បង់ទិន្នន័យពេលលុប Cache កម្មវិធី សូមធ្វើការទាញយកទិន្នន័យរក្សាទុក (Backup) ឱ្យបានទៀងទាត់។",
    btn_backup: "ទាញយកទិន្នន័យ (Backup JSON)",
    btn_restore: "បញ្ចូលទិន្នន័យឡើងវិញ (Restore JSON)",
    btn_reset_all: "លុបទិន្នន័យចោលទាំងអស់",
    mobile_nav_dashboard: "ផ្ទះ",
    mobile_nav_debtors: "អ្នកជំពាក់",
    mobile_nav_debts: "ជំពាក់",
    mobile_nav_settings: "កំណត់",
    modal_add_debtor_title: "បន្ថែមអ្នកជំពាក់ថ្មី",
    lbl_debtor_name: "ឈ្មោះអ្នកជំពាក់ *",
    ph_debtor_name: "ឧ. ឈុន ស្រីឡែន",
    lbl_debtor_phone: "លេខទូរសព្ទ",
    ph_debtor_phone: "ឧ. 098765432",
    lbl_debtor_address: "អាសយដ្ឋាន (ភូមិ) *",
    ph_debtor_address: "",
    btn_cancel: "បោះបង់",
    btn_save: "រក្សាទុក",
    modal_add_debt_title: "កត់ត្រាការជំពាក់លុយ",
    lbl_select_debtor: "ជ្រើសរើសអ្នកជំពាក់ *",
    select_placeholder: "-- សូមជ្រើសរើសអ្នកជំពាក់ --",
    lbl_items_list: "បញ្ជីទំនិញជំពាក់ (កំណត់បាន ៣ជួរ) *",
    col_item_name: "ប្រភេទអីវ៉ាន់ដែលជំពាក់",
    col_unit_price: "តម្លៃរាយ (រៀល)",
    col_qty: "ចំនួន",
    lbl_village_address: "អាសយដ្ឋាន (ភូមិ) *",
    select_village_opt: "-- ជ្រើសរើសភូមិ --",
    lbl_total_khr: "សរុបចំនួនទឹកប្រាក់ជំពាក់ (រៀល) *",
    ph_custom_item: "វាយបញ្ចូលមុខទំនិញ...",
    select_item_opt: "-- ជ្រើសរើសប្រភេទអីវ៉ាន់ --",
    select_item_opt_empty: "-- (ទទេ) --",
    lbl_debt_date: "កាលបរិច្ឆេទជំពាក់ *",
    lbl_due_date: "ថ្ងៃកំណត់សង",
    lbl_notes: "សម្គាល់បន្ថែម",
    ph_notes: "ព័ត៌មានបន្ថែមផ្សេងៗ...",
    modal_payment_title: "កត់ត្រាការសងប្រាក់",
    lbl_debtor_name_short: "អ្នកជំពាក់៖",
    lbl_debt_item_short: "មុខទំនិញ៖",
    lbl_remaining_debt: "ប្រាក់នៅខ្វះសរុប៖",
    lbl_payment_amount: "ចំនួនទឹកប្រាក់សង *",
    lbl_payment_date: "ថ្ងៃសងលុយ *",
    lbl_payment_method: "វិធីសាស្ត្រទូទាត់ *",
    method_cash: "សាច់ប្រាក់ផ្ទាល់ (Cash)",
    method_aba: "ABA Bank",
    method_acleda: "Acleda ToanChet",
    method_wing: "Wing",
    method_other: "ផ្សេងៗ",
    lbl_payment_time: "វេលាដែលបានសងលុយ",
    ph_payment_notes: "ឧ. ផ្ទេរតាម ABA ម៉ោង ២រសៀល",
    btn_pay: "សងប្រាក់",
    modal_debtor_detail_title: "ព័ត៌មានលម្អិតអ្នកជំពាក់",
    bal_usd: "ជំពាក់សរុប (USD)",
    bal_khr: "ជំពាក់សរុប (KHR)",
    btn_add_debt_short: "ថែមការជំពាក់",
    btn_share_statement: "ចម្លងរបាយការណ៍ផ្ញើតាម Telegram",
    btn_edit_debtor: "កែសម្រួលព័ត៌មាន",
    btn_print_all_unpaid: "ព្រីនរបាយការណ៍មិនទាន់សង",
    tab_active_debts: "ការជំពាក់មិនទាន់ទូទាត់",
    tab_payment_history: "ប្រវត្តិទូទាត់ប្រាក់",
    toast_saved: "រក្សាទុករួចរាល់!",
    toast_deleted: "លុបទិន្នន័យរួចរាល់!",
    toast_payment_added: "កត់ត្រាការសងប្រាក់ជោគជ័យ!",
    toast_reset_success: "បានលុបទិន្នន័យប្រព័ន្ធទាំងស្រុង!",
    toast_imported: "បញ្ចូលទិន្នន័យឡើងវិញជោគជ័យ!",
    toast_copied: "បានចម្លងរបាយការណ៍សមតុល្យ!",
    confirm_reset: "តើអ្នកពិតជាចង់លុបទិន្នន័យទាំងអស់មែនទេ? ការលុបនេះមិនអាចយកមកវិញបានឡើយ!",
    status_unpaid: "មិនទាន់សង",
    status_partial: "សងខ្លះ",
    status_paid: "សងរួច",
    action_view: "មើលលម្អិត",
    action_pay: "សងលុយ",
    action_edit: "កែសម្រួល",
    action_delete: "លុប",
    settings_auth_title: "សុវត្ថិភាព និងគណនីគ្រប់គ្រង (Account Security)",
    lbl_new_username: "ឈ្មោះអ្នកប្រើប្រាស់ថ្មី (New Username)",
    lbl_new_password: "លេខកូដសម្ងាត់ថ្មី (New Password)",
    btn_update_account: "រក្សាទុកការផ្លាស់ប្តូរ",
    btn_logout: "ចាកចេញពីប្រព័ន្ធ",
    login_title: "ចូលប្រើប្រាស់ប្រព័ន្ធ",
    login_subtitle: "ហាង សុខ ស៊ីណេត - ប្រព័ន្ធជំពាក់លុយ",
    lbl_username: "ឈ្មោះអ្នកប្រើប្រាស់ (Username)",
    lbl_password: "លេខកូដសម្ងាត់ (Password)",
    btn_login: "ចូលប្រព័ន្ធ",
    login_helper_text: "អ្នកបង្កើតប្រព័ន្ធ៖ 2026@SOK<br>ទូរសព្ទ៖ 0976111120",
    settings_logout_title: "ចាកចេញពីប្រព័ន្ធ (Log Out)",
    logout_note: "សូមចាកចេញពីប្រព័ន្ធ នៅពេលដែលអ្នកឈប់ប្រើប្រាស់ ដើម្បីការពារសុវត្ថិភាពទិន្នន័យរបស់អ្នក។",
    settings_general_title: "ការកំណត់ទូទៅ (General Settings)",
    settings_sync_title: "ភ្ជាប់ទិន្នន័យទៅកាន់ Cloud (Cloud Sync)",
    sync_status_disabled: "មិនទាន់ភ្ជាប់ (Disconnected)",
    sync_status_connecting: "កំពុងភ្ជាប់... (Connecting...)",
    sync_status_connected: "បានភ្ជាប់ (Connected - Realtime)",
    sync_status_error: "បញ្ហាការភ្ជាប់! (Connection Error)",
    btn_sync_enable: "បើកដំណើរការ (Enable)",
    btn_sync_disable: "បិទដំណើរការ (Disable)",
    lbl_sync_json_config: "បិទភ្ជាប់ Firebase Configuration (JSON) *",
    lbl_sync_shop_id: "លេខសម្គាល់ហាង / Sync Path (Shop ID)",
    btn_push_cloud: "បញ្ជូនទិន្នន័យទៅ Cloud",
    btn_save_connect: "រក្សាទុក & ភ្ជាប់",
    toast_sync_enabled: "បានបើកមុខងារ Sync ទិន្នន័យជោគជ័យ!",
    toast_sync_disabled: "បានបិទមុខងារ Sync ទិន្នន័យ!",
    toast_sync_saved: "រក្សាទុកការកំណត់ Sync ជោគជ័យ!",
    toast_push_success: "បានបញ្ជូនទិន្នន័យទៅ Cloud ជោគជ័យ!",
    confirm_push_cloud: "តើអ្នកចង់រុញទិន្នន័យក្នុងម៉ាស៊ីនបច្ចុប្បន្ន ទៅជំនួសទិន្នន័យនៅលើ Cloud មែនទេ?"
  },
  en: {
    app_title: "Sokh Sinet Shop",
    nav_dashboard: "Dashboard",
    nav_debtors: "Debtors",
    nav_debts: "Debts Ledger",
    nav_history: "Payments",
    nav_settings: "Settings",
    search_placeholder: "Search name or phone...",
    search_debtors_placeholder: "Search debtor name or phone...",
    search_debts_placeholder: "Search name or item...",
    search_history_placeholder: "Search by debtor name...",
    quick_add: "Add Debt Record",
    dashboard_title: "Analytics Dashboard",
    dashboard_subtitle: "Overview of customer debts and repayments",
    stat_total_debt: "Total Debt (Unpaid)",
    stat_total_paid: "Total Repaid (Paid)",
    stat_debtors_count: "Total Debtors",
    stat_active_debtors: "active debtors",
    chart_title: "Monthly Debt Report (in KHR)",
    legend_unpaid: "Unpaid",
    legend_paid: "Paid",
    recent_activities: "Recent Activities",
    view_all: "View All",
    no_activities: "No recent activities",
    debtors_title: "Debtors Registry",
    debtors_subtitle: "Manage customers and view detailed debt sheets",
    add_debtor_btn: "Add New Debtor",
    debts_title: "Credit Purchases Ledger",
    debts_subtitle: "Detailed item logs and outstanding balances",
    add_debt_btn: "Record New Debt",
    filter_all: "All Records",
    filter_unpaid: "Unpaid",
    filter_partial: "Partial",
    filter_paid: "Fully Paid",
    col_debtor: "Debtor Name",
    col_description: "Item / Description",
    col_amount: "Amount",
    col_remaining: "Balance",
    col_date: "Date Issued",
    col_due_date: "Due Date",
    col_status: "Status",
    col_actions: "Actions",
    col_amount_paid: "Repaid Amount",
    col_date_paid: "Repayment Date",
    col_payment_method: "Method",
    col_notes: "Time",
    col_debt_item: "Paid Against Item",
    no_debts_found: "No debt records found",
    no_payments_found: "No repayments registered yet",
    settings_title: "System Settings",
    settings_subtitle: "Configure preferences and local data security",
    settings_lang_title: "Language Preferences",
    settings_theme_title: "Visual Appearance",
    theme_light: "Light",
    theme_dark: "Dark",
    settings_data_title: "Data Backup & Maintenance",
    db_warning_note: "Data is strictly stored locally on this browser. To prevent data loss when clearing caches, backup regularly.",
    btn_backup: "Backup Database (JSON)",
    btn_restore: "Restore Database (JSON)",
    btn_reset_all: "Erase All System Data",
    mobile_nav_dashboard: "Home",
    mobile_nav_debtors: "Debtors",
    mobile_nav_debts: "Debts",
    mobile_nav_settings: "Settings",
    modal_add_debtor_title: "Add New Debtor",
    lbl_debtor_name: "Customer Name *",
    ph_debtor_name: "e.g., Chhun Sreylen",
    lbl_debtor_phone: "Phone Number",
    ph_debtor_phone: "e.g., 098765432",
    lbl_debtor_address: "Address (Village) *",
    ph_debtor_address: "",
    btn_cancel: "Cancel",
    btn_save: "Save Data",
    modal_add_debt_title: "Record Purchase on Credit",
    lbl_select_debtor: "Select Customer *",
    select_placeholder: "-- Choose Debtor --",
    lbl_items_list: "Items List (Up to 3 Rows) *",
    col_item_name: "Item Type",
    col_unit_price: "Unit Price (KHR)",
    col_qty: "Qty",
    lbl_village_address: "Address (Village) *",
    select_village_opt: "-- Select Village --",
    lbl_total_khr: "Total Debt Amount (KHR) *",
    ph_custom_item: "Enter item description...",
    select_item_opt: "-- Select Item --",
    select_item_opt_empty: "-- (Empty) --",
    lbl_debt_date: "Issue Date *",
    lbl_due_date: "Repayment Due Date",
    lbl_notes: "Additional Remarks",
    ph_notes: "Any specific notes...",
    modal_payment_title: "Record Repayment",
    lbl_debtor_name_short: "Customer:",
    lbl_debt_item_short: "Against Item:",
    lbl_remaining_debt: "Outstanding balance:",
    lbl_payment_amount: "Amount Paid *",
    lbl_payment_date: "Payment Date *",
    lbl_payment_method: "Payment Mode *",
    method_cash: "Cash In Hand",
    method_aba: "ABA Bank transfer",
    method_acleda: "Acleda ToanChet",
    method_wing: "Wing Wallet",
    method_other: "Other Methods",
    lbl_payment_time: "Time of Repayment",
    ph_payment_notes: "e.g., ABA transfer at 2:00 PM",
    btn_pay: "Pay Now",
    modal_debtor_detail_title: "Debtor File & Statement",
    bal_usd: "Balance (USD)",
    bal_khr: "Balance (KHR)",
    btn_add_debt_short: "Add Debt",
    btn_share_statement: "Copy Statement for Telegram",
    btn_edit_debtor: "Edit Details",
    btn_print_all_unpaid: "Print Unpaid Report",
    tab_active_debts: "Active Debts",
    tab_payment_history: "Payment History",
    toast_saved: "Successfully saved!",
    toast_deleted: "Record permanently deleted!",
    toast_payment_added: "Repayment logged successfully!",
    toast_reset_success: "Database cleared completely!",
    toast_imported: "Database restored successfully!",
    toast_copied: "Statement copied to clipboard!",
    confirm_reset: "Are you sure you want to clear all data? This cannot be undone!",
    status_unpaid: "Unpaid",
    status_partial: "Partial",
    status_paid: "Paid",
    action_view: "Details",
    action_pay: "Repay",
    action_edit: "Edit",
    action_delete: "Delete",
    settings_auth_title: "Account Security",
    lbl_new_username: "New Username",
    lbl_new_password: "New Password",
    btn_update_account: "Save Changes",
    btn_logout: "Log Out",
    login_title: "System Sign In",
    login_subtitle: "Sok Sinet Shop - Debt Manager",
    lbl_username: "Username",
    lbl_password: "Password",
    btn_login: "Sign In",
    login_helper_text: "System Creator: 2026@SOK<br>Tel: 0976111120",
    settings_logout_title: "Log Out of System",
    logout_note: "Please log out of the system when you are finished using it to protect your data security.",
    settings_general_title: "General Settings",
    settings_sync_title: "Cloud Sync Settings",
    sync_status_disabled: "Disconnected",
    sync_status_connecting: "Connecting...",
    sync_status_connected: "Connected (Realtime)",
    sync_status_error: "Connection Error",
    btn_sync_enable: "Enable Sync",
    btn_sync_disable: "Disable Sync",
    lbl_sync_json_config: "Paste Firebase Configuration (JSON) *",
    lbl_sync_shop_id: "Shop ID / Sync Path",
    btn_push_cloud: "Push Local to Cloud",
    btn_save_connect: "Save & Connect",
    toast_sync_enabled: "Cloud Sync enabled successfully!",
    toast_sync_disabled: "Cloud Sync disabled!",
    toast_sync_saved: "Sync settings saved successfully!",
    toast_push_success: "Pushed local data to Cloud successfully!",
    confirm_push_cloud: "Are you sure you want to overwrite Cloud data with your current local data?"
  }
};

// EXCHANGE RATE FOR VISUAL CONSOLIDATION
const EXCHANGE_RATE_USD_KHR = 4100; // 1 USD = 4100 KHR

// STATE STORAGE KEYS
const STORAGE_KEYS = {
  DEBTORS: "debt_manager_debtors_db",
  DEBTS: "debt_manager_debts_db",
  PAYMENTS: "debt_manager_payments_db",
  LANG: "debt_manager_language",
  THEME: "debt_manager_theme",
  AUTH: "debt_manager_auth_settings",
  SYNC_ENABLED: "debt_manager_sync_enabled",
  SYNC_CONFIG: "debt_manager_sync_config",
  SYNC_SHOP_ID: "debt_manager_sync_shop_id"
};

// SYSTEM SEED DATA (Cambodian Context - realistic preview)
const SEED_DATA = {
  debtors: [
    { id: "debtor_1", name: "ស៊ាង ហៃ", phone: "012888999", address: "ព្រៃម្នាស់" },
    { id: "debtor_2", name: "គឹម ឡេង", phone: "098777666", address: "ដូនណូយ" },
    { id: "debtor_3", name: "លី ដារ៉ា", phone: "077555444", address: "ស្វាយ" }
  ],
  debts: [
    { id: "debt_1", debtorId: "debtor_1", village: "ព្រៃម្នាស់", items: [{ name: "ខោទឹកនោម", price: 25000, qty: 2 }, { name: "ទឹកដោះគោ", price: 15000, qty: 3 }], item: "ខោទឹកនោម (x2), ទឹកដោះគោ (x3)", amount: 95000, currency: "KHR", date: "2026-05-10", dueDate: "2026-06-10", remaining: 45000, status: "partial" },
    { id: "debt_2", debtorId: "debtor_2", village: "ដូនណូយ", items: [{ name: "ចំណីរជ្រូក", price: 80000, qty: 5 }], item: "ចំណីរជ្រូក (x5)", amount: 400000, currency: "KHR", date: "2026-04-15", dueDate: "2026-05-15", remaining: 0, status: "paid" },
    { id: "debt_3", debtorId: "debtor_3", village: "ស្វាយ", items: [{ name: "សាច់ជ្រូក", price: 18000, qty: 4 }, { name: "ទឹកដោះគោ", price: 20000, qty: 2 }], item: "សាច់ជ្រូក (x4), ទឹកដោះគោ (x2)", amount: 112000, currency: "KHR", date: "2026-06-01", dueDate: "2026-07-01", remaining: 112000, status: "unpaid" }
  ],
  payments: [
    { id: "pay_1", debtId: "debt_1", debtorId: "debtor_1", amount: 50000, date: "2026-05-25", method: "ABA", notes: "ផ្ទេរតាម App ABA លើកទី១" },
    { id: "pay_2", debtId: "debt_2", debtorId: "debtor_2", amount: 400000, date: "2026-05-10", method: "Cash", notes: "សងសាច់ប្រាក់ដល់ហាងផ្ទាល់" }
  ]
};

// CONTROLLER STATE OBJECT
let appState = {
  debtors: [],
  debts: [],
  payments: [],
  currentLang: "km",
  currentTheme: "light",
  currentView: "dashboard",
  searchQuery: "",
  activeFilter: "all",
  isLoggedIn: false
};

// SYSTEM SECURITY & AUTO-LOGOUT
let lastActiveTime = Date.now();
let inactivityCheckInterval = null;

function getAuthSettings() {
  const localAuth = localStorage.getItem(STORAGE_KEYS.AUTH);
  if (!localAuth) {
    const defaultAuth = { username: "admin", password: "123" };
    localStorage.setItem(STORAGE_KEYS.AUTH, JSON.stringify(defaultAuth));
    return defaultAuth;
  }
  try {
    return JSON.parse(localAuth) || { username: "admin", password: "123" };
  } catch (e) {
    return { username: "admin", password: "123" };
  }
}

function saveAuthSettings(username, password) {
  const auth = { username, password };
  localStorage.setItem(STORAGE_KEYS.AUTH, JSON.stringify(auth));
}

function initAuth() {
  getAuthSettings();
  const isLoggedIn = sessionStorage.getItem("is_logged_in") === "true";
  const lastActiveStr = sessionStorage.getItem("last_active_time");
  
  if (isLoggedIn && lastActiveStr) {
    const elapsed = Date.now() - parseInt(lastActiveStr, 10);
    if (elapsed > 5 * 60 * 1000) { // 5 minutes
      logoutUser(false);
      return;
    }
  }

  if (isLoggedIn) {
    loginUserSuccess();
  } else {
    showLoginOverlay();
  }
}

function showLoginOverlay() {
  appState.isLoggedIn = false;
  const appContainer = document.querySelector(".app-container");
  const loginOverlay = document.getElementById("login-overlay");
  if (appContainer) appContainer.style.display = "none";
  if (loginOverlay) {
    loginOverlay.style.display = "flex";
    document.getElementById("login-error-msg").style.display = "none";
    document.getElementById("login-username").value = "";
    document.getElementById("login-password").value = "";
  }
}

function loginUserSuccess() {
  appState.isLoggedIn = true;
  sessionStorage.setItem("is_logged_in", "true");
  lastActiveTime = Date.now();
  sessionStorage.setItem("last_active_time", lastActiveTime);
  
  const appContainer = document.querySelector(".app-container");
  const loginOverlay = document.getElementById("login-overlay");
  if (appContainer) appContainer.style.display = "flex";
  if (loginOverlay) loginOverlay.style.display = "none";
  
  renderCurrentView();
  startInactivityTimer();
}

function logoutUser(showFeedback = true) {
  appState.isLoggedIn = false;
  sessionStorage.removeItem("is_logged_in");
  sessionStorage.removeItem("last_active_time");
  stopInactivityTimer();
  showLoginOverlay();
  
  if (showFeedback) {
    showToast(appState.currentLang === "km" ? "បានចាកចេញពីប្រព័ន្ធ!" : "Successfully logged out!");
  }
}

function resetInactivityTimer() {
  if (appState.isLoggedIn) {
    lastActiveTime = Date.now();
    sessionStorage.setItem("last_active_time", lastActiveTime);
  }
}

function startInactivityTimer() {
  stopInactivityTimer();
  ["click", "mousemove", "keydown", "scroll", "touchstart"].forEach(evt => {
    window.addEventListener(evt, resetInactivityTimer, { passive: true });
  });
  
  inactivityCheckInterval = setInterval(() => {
    if (appState.isLoggedIn) {
      const elapsed = Date.now() - lastActiveTime;
      if (elapsed > 5 * 60 * 1000) { // 5 minutes
        logoutUser(false);
        showToast(appState.currentLang === "km" ? "គណនីត្រូវបានចាកចេញដោយស្វ័យប្រវត្តិតាមរយៈការមិនប្រើប្រាស់រយៈពេល ៥នាទី!" : "Session expired due to 5 minutes of inactivity!");
      }
    }
  }, 5000);
}

function stopInactivityTimer() {
  ["click", "mousemove", "keydown", "scroll", "touchstart"].forEach(evt => {
    window.removeEventListener(evt, resetInactivityTimer);
  });
  if (inactivityCheckInterval) {
    clearInterval(inactivityCheckInterval);
    inactivityCheckInterval = null;
  }
}

// INITIALIZATION FUNCTIONS
document.addEventListener("DOMContentLoaded", () => {
  initDatabase();
  initTheme();
  initLanguage();
  initNavigation();
  initEventHandlers();
  initAuth();
  initCloudSync(); // Initialize Firebase Cloud Sync
  
  // Register service worker if available
  registerServiceWorker();
  
  // Start the 5-minute auto-refresh timer
  startAutoRefreshTimer();
});

// SERVICE WORKER FOR OFFLINE PWA & AUTO-REFRESH SCRIPT
let autoRefreshInterval = null;

function registerServiceWorker() {
  if ("serviceWorker" in navigator) {
    window.addEventListener("load", () => {
      navigator.serviceWorker.register("sw.js")
        .then(reg => {
          console.log("Service Worker registered!", reg.scope);
          // Initial check for updates on load
          checkSWUpdate();
        })
        .catch(err => console.log("Service Worker registration failed: ", err));
    });
  }
}

function checkSWUpdate() {
  if ("serviceWorker" in navigator) {
    navigator.serviceWorker.ready.then((reg) => {
      // Force update check from server
      reg.update().catch(err => console.log("Failed to update SW:", err));
      
      reg.onupdatefound = () => {
        const installingWorker = reg.installing;
        if (installingWorker) {
          installingWorker.onstatechange = () => {
            if (installingWorker.state === "installed") {
              if (navigator.serviceWorker.controller) {
                console.log("New Service Worker version detected!");
                showToast(appState.currentLang === "km" 
                  ? "មានកំណែទម្រង់ថ្មី! កំពុងកែសម្រួលប្រព័ន្ធ..." 
                  : "New version available! Refreshing...");
                setTimeout(() => {
                  window.location.reload();
                }, 2000);
              }
            }
          };
        }
      };
    });
  }
}

function startAutoRefreshTimer() {
  if (autoRefreshInterval) {
    clearInterval(autoRefreshInterval);
  }
  
  // Every 5 minutes (5 * 60 * 1000 ms)
  autoRefreshInterval = setInterval(() => {
    // 1. First trigger Service Worker update check
    checkSWUpdate();
    
    // 2. Refresh local database and view, or reload page if inactive and not editing
    const activeModals = document.querySelectorAll(".modal-overlay.active");
    const activeLogin = document.getElementById("login-overlay") && document.getElementById("login-overlay").style.display === "flex";
    const activeElement = document.activeElement;
    const isTyping = activeElement && (activeElement.tagName === "INPUT" || activeElement.tagName === "TEXTAREA" || activeElement.tagName === "SELECT");

    if (activeModals.length === 0 && !activeLogin && !isTyping) {
      console.log("Auto-refresh: checking inactivity for page reload");
      
      // If user has been inactive for at least 15 seconds, reload the page to fully synchronize code/data
      const timeSinceLastActive = Date.now() - lastActiveTime;
      if (timeSinceLastActive > 15 * 1000) {
        console.log("Forcing page reload for synchronization...");
        window.location.reload();
      } else {
        // Otherwise, just do a silent refresh of the UI and data in background
        initDatabase();
        renderCurrentView();
      }
    }
  }, 5 * 60 * 1000);
}

// STORAGE ENGINE
function initDatabase() {
  const localDebtors = localStorage.getItem(STORAGE_KEYS.DEBTORS);
  const localDebts = localStorage.getItem(STORAGE_KEYS.DEBTS);
  const localPayments = localStorage.getItem(STORAGE_KEYS.PAYMENTS);

  if (!localDebtors && !localDebts && !localPayments) {
    // Seed database on first run
    appState.debtors = [...SEED_DATA.debtors];
    appState.debts = [...SEED_DATA.debts];
    appState.payments = [...SEED_DATA.payments];
    saveToLocalStorage();
  } else {
    appState.debtors = JSON.parse(localDebtors) || [];
    appState.debts = JSON.parse(localDebts) || [];
    appState.payments = JSON.parse(localPayments) || [];
  }
}

function saveToLocalStorage() {
  localStorage.setItem(STORAGE_KEYS.DEBTORS, JSON.stringify(appState.debtors));
  localStorage.setItem(STORAGE_KEYS.DEBTS, JSON.stringify(appState.debts));
  localStorage.setItem(STORAGE_KEYS.PAYMENTS, JSON.stringify(appState.payments));
  
  // Sync to cloud if enabled
  syncDataToCloud();
}

// VISUAL MODE SWITCHER
function initTheme() {
  const storedTheme = localStorage.getItem(STORAGE_KEYS.THEME) || "light";
  setTheme(storedTheme);
}

function setTheme(themeName) {
  appState.currentTheme = themeName;
  localStorage.setItem(STORAGE_KEYS.THEME, themeName);
  
  document.body.className = `theme-${themeName}`;
  
  // Update toggles in Settings
  document.querySelectorAll(".theme-btn-select").forEach(btn => {
    btn.classList.toggle("active", btn.dataset.theme === themeName);
  });
}

// LANGUAGE SWITCHER
function initLanguage() {
  const storedLang = localStorage.getItem(STORAGE_KEYS.LANG) || "km";
  setLanguage(storedLang);
}

function setLanguage(langCode) {
  appState.currentLang = langCode;
  localStorage.setItem(STORAGE_KEYS.LANG, langCode);
  
  // Translate UI elements using data-i18n attributes
  document.querySelectorAll("[data-i18n]").forEach(el => {
    const key = el.getAttribute("data-i18n");
    if (i18n[langCode][key]) {
      if (i18n[langCode][key].includes("<br>") || i18n[langCode][key].includes("<span")) {
        el.innerHTML = i18n[langCode][key];
      } else {
        el.textContent = i18n[langCode][key];
      }
    }
  });

  // Translate placeholders
  document.querySelectorAll("[data-i18n-placeholder]").forEach(el => {
    const key = el.getAttribute("data-i18n-placeholder");
    if (i18n[langCode][key]) {
      el.setAttribute("placeholder", i18n[langCode][key]);
    }
  });

  // Update top bar text & options active selection
  const langToggle = document.getElementById("lang-toggle");
  if (langToggle) langToggle.textContent = langCode === "km" ? "EN" : "ខ្មែរ";

  document.querySelectorAll(".lang-btn-select").forEach(btn => {
    btn.classList.toggle("active", btn.dataset.lang === langCode);
  });

  // Re-render forms / dynamic details
  populateDebtorDropdowns();
  
  if (document.getElementById(`view-${appState.currentView}`).style.display !== "none" || true) {
    renderCurrentView();
  }
}

// ROUTER MANAGEMENT
function initNavigation() {
  const handleNavClick = (viewName) => {
    appState.currentView = viewName;
    
    // Update navigation items active state
    document.querySelectorAll(".nav-item").forEach(item => {
      item.classList.toggle("active", item.dataset.view === viewName);
    });
    document.querySelectorAll(".mobile-nav-item").forEach(item => {
      item.classList.toggle("active", item.dataset.view === viewName);
    });

    renderCurrentView();
  };

  document.querySelectorAll(".nav-item").forEach(btn => {
    btn.addEventListener("click", () => handleNavClick(btn.dataset.view));
  });

  document.querySelectorAll(".mobile-nav-item").forEach(btn => {
    btn.addEventListener("click", () => handleNavClick(btn.dataset.view));
  });
  
  // Deep link from dashboard buttons
  document.addEventListener("click", (e) => {
    const actionEl = e.target.closest("[data-action]");
    if (actionEl && actionEl.dataset.action === "view-debts") {
      handleNavClick("debts");
    }
  });
}

function renderCurrentView() {
  // Hide all views, display the selected one
  document.querySelectorAll(".app-view").forEach(view => {
    view.classList.remove("active");
  });
  
  const targetView = document.getElementById(`view-${appState.currentView}`);
  if (targetView) targetView.classList.add("active");

  // Perform view-specific rendering
  switch (appState.currentView) {
    case "dashboard":
      renderDashboard();
      break;
    case "debtors":
      renderDebtorsList();
      break;
    case "debts":
      renderDebtsLedger();
      break;
    case "history":
      renderPaymentHistory();
      break;
    case "settings":
      const auth = getAuthSettings();
      document.getElementById("change-username").value = auth.username;
      document.getElementById("change-password").value = auth.password;
      const msgSpan = document.getElementById("change-auth-msg");
      if (msgSpan) msgSpan.textContent = "";
      break;
  }
}

// DYNAMIC DASHBOARD RENDERER
function renderDashboard() {
  // 1. Calculate balances
  let unpaidUSD = 0;
  let unpaidKHR = 0;
  let paidUSD = 0;
  let paidKHR = 0;
  let activeDebtorsSet = new Set();

  appState.debts.forEach(d => {
    const isUnpaid = d.status === "unpaid" || d.status === "partial";
    const isPaid = d.status === "paid";
    
    // Unpaid/Remaining calculations
    if (d.currency === "USD") {
      unpaidUSD += d.remaining;
      paidUSD += (d.amount - d.remaining);
    } else {
      unpaidKHR += d.remaining;
      paidKHR += (d.amount - d.remaining);
    }

    if (d.remaining > 0) {
      activeDebtorsSet.add(d.debtorId);
    }
  });

  // Write stats values
  document.getElementById("stat-unpaid-usd").textContent = formatCurrency(unpaidUSD, "USD");
  document.getElementById("stat-unpaid-khr").textContent = formatCurrency(unpaidKHR, "KHR");
  document.getElementById("stat-paid-usd").textContent = formatCurrency(paidUSD, "USD");
  document.getElementById("stat-paid-khr").textContent = formatCurrency(paidKHR, "KHR");
  document.getElementById("stat-debtors-count").textContent = activeDebtorsSet.size;

  // 2. Render Activity list (5 most recent activities)
  renderRecentActivities();

  // 3. Render Custom SVG Monthly Chart
  renderSVGChart();
}

function renderRecentActivities() {
  const container = document.getElementById("recent-activity-list");
  container.innerHTML = "";

  // Combine debts and payments into activities sorted by date desc
  let activities = [];

  appState.debts.forEach(d => {
    const debtor = appState.debtors.find(dr => dr.id === d.debtorId);
    activities.push({
      type: "debt",
      title: debtor ? debtor.name : "Unknown Customer",
      subtitle: d.item,
      amount: d.amount,
      currency: d.currency,
      date: d.date,
      rawDate: new Date(d.date)
    });
  });

  appState.payments.forEach(p => {
    const debtor = appState.debtors.find(dr => dr.id === p.debtorId);
    const debt = appState.debts.find(d => d.id === p.debtId);
    activities.push({
      type: "payment",
      title: debtor ? debtor.name : "Unknown Customer",
      subtitle: debt ? `${i18n[appState.currentLang].action_pay} - ${debt.item}` : i18n[appState.currentLang].action_pay,
      amount: p.amount,
      currency: debt ? debt.currency : "USD",
      date: p.date,
      rawDate: new Date(p.date)
    });
  });

  // Sort: most recent first
  activities.sort((a, b) => b.rawDate - a.rawDate);
  const recent = activities.slice(0, 5);

  if (recent.length === 0) {
    container.innerHTML = `<div class="empty-state"><span>${i18n[appState.currentLang].no_activities}</span></div>`;
    return;
  }

  recent.forEach(act => {
    const item = document.createElement("div");
    item.className = `activity-item type-${act.type}`;
    
    const formattedAmount = formatCurrency(act.amount, act.currency);
    const amtClass = act.type === "debt" ? "debt" : "payment";
    const amtSymbol = act.type === "debt" ? "+" : "-";

    item.innerHTML = `
      <div class="act-desc">
        <span class="act-title">${act.title}</span>
        <span class="act-subtitle">${act.subtitle}</span>
        <span class="act-subtitle" style="font-family: var(--font-numbers);">${act.date}</span>
      </div>
      <div class="act-amount ${amtClass}">
        ${amtSymbol}${formattedAmount}
      </div>
    `;
    container.appendChild(item);
  });
}

// SVG BAR GRAPH GENERATOR (Pure Dynamic SVG)
function renderSVGChart() {
  const svg = document.getElementById("dashboard-svg-chart");
  const xLabelsContainer = document.getElementById("chart-labels-x");
  
  if (!svg || !xLabelsContainer) return;
  
  svg.innerHTML = "";
  xLabelsContainer.innerHTML = "";

  // 1. Group debts by Month (Last 6 Months)
  const monthNames = appState.currentLang === "km" ? 
    ["មករា", "កុម្ភៈ", "មីនា", "មេសា", "ឧសភា", "មិថុនា", "កក្កដា", "សីហា", "កញ្ញា", "តុលា", "វិច្ឆិកា", "ធ្នូ"] :
    ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"];

  let monthlyData = [];
  const today = new Date();
  
  for (let i = 5; i >= 0; i--) {
    let d = new Date(today.getFullYear(), today.getMonth() - i, 1);
    monthlyData.push({
      year: d.getFullYear(),
      month: d.getMonth(),
      label: monthNames[d.getMonth()],
      unpaid: 0, // Consolidated in USD
      paid: 0
    });
  }

  // Aggregate values
  appState.debts.forEach(d => {
    const debtDate = new Date(d.date);
    const debtMonthIndex = monthlyData.findIndex(m => m.year === debtDate.getFullYear() && m.month === debtMonthIndexCalc(debtDate));
    
    // Help calculate matching index
    function debtMonthIndexCalc(date) { return date.getMonth(); }
    
    const matchedMonth = monthlyData.find(m => m.year === debtDate.getFullYear() && m.month === debtDate.getMonth());
    
    if (matchedMonth) {
      // Consolidate amount to KHR for graphing height consistency
      let khrAmount = d.currency === "KHR" ? d.amount : d.amount * EXCHANGE_RATE_USD_KHR;
      let khrRemaining = d.currency === "KHR" ? d.remaining : d.remaining * EXCHANGE_RATE_USD_KHR;
      let khrPaid = khrAmount - khrRemaining;

      matchedMonth.unpaid += khrRemaining;
      matchedMonth.paid += khrPaid;
    }
  });

  // Helper to format values for compact chart labels
  function formatChartValue(val) {
    if (val >= 1000000) {
      return (val / 1000000).toFixed(1).replace(/\.0$/, "") + "M";
    }
    if (val >= 1000) {
      return (val / 1000).toFixed(0) + "K";
    }
    return Math.round(val);
  }

  // 2. Find max value for scaling
  let maxVal = 100000; // default min height scale in KHR (100K Riel)
  monthlyData.forEach(m => {
    const tot = m.unpaid + m.paid;
    if (tot > maxVal) maxVal = tot;
  });
  
  // Round up maxVal for visual headroom
  maxVal = Math.ceil(maxVal * 1.15);

  // SVG parameters
  const width = 500;
  const height = 200;
  const paddingLeft = 55; // Slightly increased for riel symbol
  const paddingRight = 20;
  const paddingTop = 20;
  const paddingBottom = 30;
  const graphHeight = height - paddingTop - paddingBottom;
  const graphWidth = width - paddingLeft - paddingRight;

  // Draw Horizontal Gridlines (4 lines)
  for (let i = 0; i <= 4; i++) {
    const yVal = paddingTop + (graphHeight / 4) * i;
    const gridVal = maxVal - ((maxVal / 4) * i);
    
    // Line
    const line = document.createElementNS("http://www.w3.org/2000/svg", "line");
    line.setAttribute("x1", paddingLeft);
    line.setAttribute("y1", yVal);
    line.setAttribute("x2", width - paddingRight);
    line.setAttribute("y2", yVal);
    line.setAttribute("class", "chart-grid-line");
    svg.appendChild(line);

    // Y Axis Label
    const text = document.createElementNS("http://www.w3.org/2000/svg", "text");
    text.setAttribute("x", paddingLeft - 8);
    text.setAttribute("y", yVal + 4);
    text.setAttribute("class", "chart-text");
    text.setAttribute("style", "text-anchor: end; font-weight: 500;");
    text.textContent = `${formatChartValue(gridVal)} ៛`;
    svg.appendChild(text);
  }

  // Draw Bars
  const numMonths = monthlyData.length;
  const colWidth = graphWidth / numMonths;
  const barWidth = colWidth * 0.4;

  monthlyData.forEach((m, idx) => {
    const xCenter = paddingLeft + (colWidth * idx) + (colWidth / 2);
    
    // Heights
    const unpaidHeight = (m.unpaid / maxVal) * graphHeight;
    const paidHeight = (m.paid / maxVal) * graphHeight;

    const yUnpaid = height - paddingBottom - unpaidHeight;
    const yPaid = yUnpaid - paidHeight; // Stacked chart approach for clean aesthetic

    // Draw Paid Bar (Bottom portion of stacked)
    if (m.paid > 0) {
      const rectPaid = document.createElementNS("http://www.w3.org/2000/svg", "rect");
      rectPaid.setAttribute("x", xCenter - barWidth/2);
      rectPaid.setAttribute("y", height - paddingBottom - paidHeight);
      rectPaid.setAttribute("width", barWidth);
      rectPaid.setAttribute("height", paidHeight);
      rectPaid.setAttribute("class", "chart-bar-paid");
      
      // Simple tooltip title
      const title = document.createElementNS("http://www.w3.org/2000/svg", "title");
      title.textContent = `Paid: ${formatCurrency(m.paid, "KHR")}`;
      rectPaid.appendChild(title);
      svg.appendChild(rectPaid);
    }

    // Draw Unpaid Bar (Top portion of stacked)
    if (m.unpaid > 0) {
      const rectUnpaid = document.createElementNS("http://www.w3.org/2000/svg", "rect");
      rectUnpaid.setAttribute("x", xCenter - barWidth/2);
      rectUnpaid.setAttribute("y", height - paddingBottom - paidHeight - unpaidHeight);
      rectUnpaid.setAttribute("width", barWidth);
      rectUnpaid.setAttribute("height", unpaidHeight);
      rectUnpaid.setAttribute("class", "chart-bar-unpaid");
      
      const title = document.createElementNS("http://www.w3.org/2000/svg", "title");
      title.textContent = `Unpaid: ${formatCurrency(m.unpaid, "KHR")}`;
      rectUnpaid.appendChild(title);
      svg.appendChild(rectUnpaid);
    }

    // Render bar value text labels if there's any values
    const total = m.unpaid + m.paid;
    if (total > 0) {
      const textVal = document.createElementNS("http://www.w3.org/2000/svg", "text");
      textVal.setAttribute("x", xCenter);
      textVal.setAttribute("y", height - paddingBottom - paidHeight - unpaidHeight - 5);
      textVal.setAttribute("class", "chart-text-value");
      textVal.textContent = formatChartValue(total);
      svg.appendChild(textVal);
    }

    // Label on HTML container
    const xLabel = document.createElement("div");
    xLabel.className = "chart-label-x-item";
    xLabel.textContent = m.label;
    xLabelsContainer.appendChild(xLabel);
  });
}

// DEBTORS LIST MANAGER
function renderDebtorsList() {
  const container = document.getElementById("debtors-list-container");
  container.innerHTML = "";

  const query = appState.searchQuery.toLowerCase().trim();
  
  const filteredDebtors = appState.debtors.filter(d => {
    const matchesName = d.name.toLowerCase().includes(query);
    const matchesPhone = d.phone.includes(query);
    return matchesName || matchesPhone;
  });

  if (filteredDebtors.length === 0) {
    container.innerHTML = `
      <div class="card col-span-2 text-center text-muted" style="padding: 40px; grid-column: 1 / -1;">
        <span data-i18n="no_debts_found">${i18n[appState.currentLang].no_debts_found}</span>
      </div>`;
    return;
  }

  // Sort debtors: active debtors (outstanding > 0) on top, paid debtors at the bottom.
  // Active debtors are sorted chronologically by their earliest active debt date (oldest first).
  // Paid debtors are sorted by their latest debt date descending (newest first).
  filteredDebtors.sort((a, b) => {
    const getStatusAndDate = (debtor) => {
      const debts = appState.debts.filter(d => d.debtorId === debtor.id);
      const activeDebts = debts.filter(d => d.remaining > 0);
      const hasOutstanding = activeDebts.length > 0;
      
      let repDate = 0;
      if (hasOutstanding) {
        const times = activeDebts.map(d => new Date(d.date).getTime());
        repDate = Math.min(...times);
      } else if (debts.length > 0) {
        const times = debts.map(d => new Date(d.date).getTime());
        repDate = Math.max(...times);
      }
      
      return { hasOutstanding, repDate };
    };

    const infoA = getStatusAndDate(a);
    const infoB = getStatusAndDate(b);

    // Active first, paid last
    if (infoA.hasOutstanding && !infoB.hasOutstanding) return -1;
    if (!infoA.hasOutstanding && infoB.hasOutstanding) return 1;

    // Within same group:
    if (infoA.hasOutstanding) {
      // Active debtors: oldest borrowing date first (ascending)
      return infoA.repDate - infoB.repDate;
    } else {
      // Paid debtors: newest borrowing date first (descending)
      return infoB.repDate - infoA.repDate;
    }
  });

  filteredDebtors.forEach(d => {
    // Calculate total debt for this debtor
    let totalUSD = 0;
    let totalKHR = 0;

    appState.debts.forEach(debt => {
      if (debt.debtorId === d.id && debt.remaining > 0) {
        if (debt.currency === "USD") {
          totalUSD += debt.remaining;
        } else {
          totalKHR += debt.remaining;
        }
      }
    });

    const initial = d.name.trim().charAt(0);
    const card = document.createElement("div");
    card.className = "debtor-card";
    
    const balanceUSDText = totalUSD > 0 ? `$${totalUSD.toFixed(2)}` : "0.00";
    const balanceKHRText = totalKHR > 0 ? `${totalKHR.toLocaleString()} ៛` : "0 ៛";
    
    const balanceUSDClass = totalUSD > 0 ? "debt text-danger" : "none";
    const balanceKHRClass = totalKHR > 0 ? "debt text-danger" : "none";

    card.innerHTML = `
      <div class="debtor-card-header">
        <div class="debtor-avatar">${initial}</div>
        <div class="debtor-name-phone">
          <span class="debtor-name">${d.name}${d.address ? ` (${d.address})` : ""}</span>
          <span class="debtor-phone">${d.phone || "---"}</span>
        </div>
      </div>
      
      <div class="debtor-balance-box">
        <div class="debtor-balance-item">
          <span class="debtor-balance-label">USD Balance</span>
          <span class="debtor-balance-value ${balanceUSDClass}">${balanceUSDText}</span>
        </div>
        <div class="debtor-balance-item">
          <span class="debtor-balance-label">KHR Balance</span>
          <span class="debtor-balance-value ${balanceKHRClass}">${balanceKHRText}</span>
        </div>
      </div>
      
      <div class="debtor-card-footer">
        <button class="btn btn-outline btn-sm btn-view-debtor" data-id="${d.id}" title="${i18n[appState.currentLang].action_view}">
          ${i18n[appState.currentLang].action_view}
        </button>
        <button class="btn btn-outline btn-sm btn-print-debtor" data-id="${d.id}" title="${appState.currentLang === 'km' ? 'បោះពុម្ពរបាយការណ៍' : 'Print Statement'}">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" style="width: 1.1rem; height: 1.1rem;">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6.72 13.821V21h10.56v-7.179m-12 0A2.25 2.25 0 0 1 3 11.58V8.71a2.25 2.25 0 0 1 2.25-2.25h13.5A2.25 2.25 0 0 1 21 8.71v2.87a2.25 2.25 0 0 1-2.28 2.241m-12 0h12m-12 0v-1.8c0-.621.504-1.125 1.125-1.125h9.75c.621 0 1.125.504 1.125 1.125v1.8M8.25 6.75h7.5M12 9h.008v.008H12V9Zm.375 0a.375.375 0 1 1-.75 0 .375.375 0 0 1 .75 0Z" />
          </svg>
        </button>
        <button class="btn btn-primary btn-sm btn-add-debt-for" data-id="${d.id}">
          + ${i18n[appState.currentLang].nav_debts}
        </button>
      </div>
    `;
    
    // Attach handlers
    card.querySelector(".btn-view-debtor").addEventListener("click", () => {
      openDebtorDetailModal(d.id);
    });

    card.querySelector(".btn-print-debtor").addEventListener("click", () => {
      printDebtorStatement(d.id);
    });

    card.querySelector(".btn-add-debt-for").addEventListener("click", () => {
      openAddDebtModal(d.id);
    });

    container.appendChild(card);
  });
}

// DEBTS LEDGER MANAGER
function renderDebtsLedger() {
  const tbody = document.getElementById("debts-list-tbody");
  const mobileList = document.getElementById("debts-mobile-list");
  const emptyState = document.getElementById("debts-empty-state");
  
  tbody.innerHTML = "";
  mobileList.innerHTML = "";

  const query = appState.searchQuery.toLowerCase().trim();
  const filter = appState.activeFilter; // all, unpaid, partial, paid

  const filteredDebts = appState.debts.filter(d => {
    const debtor = appState.debtors.find(dr => dr.id === d.debtorId);
    const debtorName = debtor ? debtor.name.toLowerCase() : "";
    const itemDesc = d.item.toLowerCase();
    
    const matchesSearch = debtorName.includes(query) || itemDesc.includes(query);
    
    let matchesFilter = true;
    if (filter === "unpaid") matchesFilter = d.status === "unpaid";
    else if (filter === "partial") matchesFilter = d.status === "partial";
    else if (filter === "paid") matchesFilter = d.status === "paid";
    
    return matchesSearch && matchesFilter;
  });

  // Show/Hide Empty State
  if (filteredDebts.length === 0) {
    emptyState.style.display = "flex";
    return;
  } else {
    emptyState.style.display = "none";
  }

  // Sort Debts: Unpaid first, then by date desc
  filteredDebts.sort((a, b) => {
    if (a.status !== "paid" && b.status === "paid") return -1;
    if (a.status === "paid" && b.status !== "paid") return 1;
    return new Date(b.date) - new Date(a.date);
  });

  filteredDebts.forEach(d => {
    const debtor = appState.debtors.find(dr => dr.id === d.debtorId);
    const debtorName = debtor ? (debtor.name + (debtor.address ? ` (${debtor.address})` : "")) : "Unknown Customer";

    // 1. Desktop Table Row
    const tr = document.createElement("tr");
    
    const badgeClass = `badge-${d.status}`;
    const statusText = i18n[appState.currentLang][`status_${d.status}`];
    
    const isPaid = d.status === "paid";
    const payBtnHtml = isPaid ? "" : `
      <button class="btn btn-success btn-sm btn-pay-debt-row" data-id="${d.id}">
        ${i18n[appState.currentLang].action_pay}
      </button>
    `;

    tr.innerHTML = `
      <td><strong>${debtorName}</strong></td>
      <td>${d.item}</td>
      <td class="val-amount">${formatCurrency(d.amount, d.currency)}</td>
      <td class="val-amount text-danger">${formatCurrency(d.remaining, d.currency)}</td>
      <td class="val-date">${d.date}</td>
      <td class="val-date">${d.dueDate || "---"}</td>
      <td><span class="badge ${badgeClass}">${statusText}</span></td>
      <td class="text-right">
        <div class="table-actions-flex">
          ${payBtnHtml}
          <button class="btn btn-outline btn-sm btn-edit-debt-row" data-id="${d.id}">✏️</button>
          <button class="btn btn-outline btn-sm border-danger text-danger btn-delete-debt-row" data-id="${d.id}">🗑️</button>
        </div>
      </td>
    `;

    // Add events
    if (!isPaid) {
      tr.querySelector(".btn-pay-debt-row").addEventListener("click", () => openPaymentModal(d.id));
    }
    tr.querySelector(".btn-edit-debt-row").addEventListener("click", () => openEditDebtModal(d.id));
    tr.querySelector(".btn-delete-debt-row").addEventListener("click", () => deleteDebt(d.id));

    tbody.appendChild(tr);

    // 2. Mobile Responsive Cards
    const mCard = document.createElement("div");
    mCard.className = "mobile-card-item";
    mCard.innerHTML = `
      <div class="mobile-card-row">
        <span class="mobile-card-val bold text-indigo">${debtorName}</span>
        <span class="badge ${badgeClass}">${statusText}</span>
      </div>
      <div class="mobile-card-row">
        <span class="mobile-card-lbl">${i18n[appState.currentLang].col_description}</span>
        <span class="mobile-card-val">${d.item}</span>
      </div>
      <div class="mobile-card-row">
        <span class="mobile-card-lbl">${i18n[appState.currentLang].col_amount}</span>
        <span class="mobile-card-val amount">${formatCurrency(d.amount, d.currency)}</span>
      </div>
      <div class="mobile-card-row">
        <span class="mobile-card-lbl">${i18n[appState.currentLang].col_remaining}</span>
        <span class="mobile-card-val amount text-danger">${formatCurrency(d.remaining, d.currency)}</span>
      </div>
      <div class="mobile-card-row">
        <span class="mobile-card-lbl">${i18n[appState.currentLang].col_date}</span>
        <span class="mobile-card-val">${d.date}</span>
      </div>
      <div class="mobile-card-actions">
        ${isPaid ? "" : `<button class="btn btn-success btn-sm btn-pay-debt-mob" data-id="${d.id}">${i18n[appState.currentLang].action_pay}</button>`}
        <button class="btn btn-outline btn-sm btn-edit-debt-mob" data-id="${d.id}">✏️</button>
        <button class="btn btn-outline btn-sm border-danger text-danger btn-delete-debt-mob" data-id="${d.id}">🗑️</button>
      </div>
    `;

    // Mobile Actions binding
    if (!isPaid) {
      mCard.querySelector(".btn-pay-debt-mob").addEventListener("click", () => openPaymentModal(d.id));
    }
    mCard.querySelector(".btn-edit-debt-mob").addEventListener("click", () => openEditDebtModal(d.id));
    mCard.querySelector(".btn-delete-debt-mob").addEventListener("click", () => deleteDebt(d.id));

    mobileList.appendChild(mCard);
  });
}

// REPAYMENTS LOG PAGE
function renderPaymentHistory() {
  const tbody = document.getElementById("payments-list-tbody");
  const mobileList = document.getElementById("payments-mobile-list");
  const emptyState = document.getElementById("payments-empty-state");
  
  tbody.innerHTML = "";
  mobileList.innerHTML = "";

  const query = appState.searchQuery.toLowerCase().trim();

  const filteredPayments = appState.payments.filter(p => {
    const debtor = appState.debtors.find(dr => dr.id === p.debtorId);
    const debtorName = debtor ? debtor.name.toLowerCase() : "";
    return debtorName.includes(query);
  });

  if (filteredPayments.length === 0) {
    emptyState.style.display = "flex";
    return;
  } else {
    emptyState.style.display = "none";
  }

  // Sort: payment date desc
  filteredPayments.sort((a, b) => new Date(b.date) - new Date(a.date));

  filteredPayments.forEach(p => {
    const debtor = appState.debtors.find(dr => dr.id === p.debtorId);
    const debtorName = debtor ? debtor.name : "Unknown Customer";

    const debt = appState.debts.find(d => d.id === p.debtId);
    const itemDesc = debt ? debt.item : "---";
    const currency = debt ? debt.currency : "USD";

    // 1. Desktop Row
    const tr = document.createElement("tr");
    tr.innerHTML = `
      <td><strong>${debtorName}</strong></td>
      <td>${itemDesc}</td>
      <td class="val-amount text-emerald">${formatCurrency(p.amount, currency)}</td>
      <td class="val-date">${p.date}</td>
      <td><span class="badge badge-paid">${p.method}</span></td>
      <td><span class="text-secondary">${p.notes || "---"}</span></td>
      <td class="text-right">
        <button class="btn btn-outline btn-sm border-danger text-danger btn-delete-pay-row" data-id="${p.id}">🗑️</button>
      </td>
    `;
    
    tr.querySelector(".btn-delete-pay-row").addEventListener("click", () => deletePayment(p.id));
    tbody.appendChild(tr);

    // 2. Mobile Responsive Card
    const mCard = document.createElement("div");
    mCard.className = "mobile-card-item";
    mCard.innerHTML = `
      <div class="mobile-card-row">
        <span class="mobile-card-val bold text-emerald">${debtorName}</span>
        <span class="badge badge-paid">${p.method}</span>
      </div>
      <div class="mobile-card-row">
        <span class="mobile-card-lbl">${i18n[appState.currentLang].col_debt_item}</span>
        <span class="mobile-card-val">${itemDesc}</span>
      </div>
      <div class="mobile-card-row">
        <span class="mobile-card-lbl">${i18n[appState.currentLang].col_amount_paid}</span>
        <span class="mobile-card-val amount text-emerald">${formatCurrency(p.amount, currency)}</span>
      </div>
      <div class="mobile-card-row">
        <span class="mobile-card-lbl">${i18n[appState.currentLang].col_date_paid}</span>
        <span class="mobile-card-val">${p.date}</span>
      </div>
      <div class="mobile-card-row" style="border-top: 1px dashed var(--border-color); padding-top: 8px;">
        <span class="mobile-card-lbl">${i18n[appState.currentLang].col_notes}</span>
        <span class="mobile-card-val text-secondary">${p.notes || "---"}</span>
      </div>
      <div class="mobile-card-actions">
        <button class="btn btn-outline btn-sm border-danger text-danger btn-delete-pay-mob" data-id="${p.id}">🗑️ ${i18n[appState.currentLang].action_delete}</button>
      </div>
    `;

    mCard.querySelector(".btn-delete-pay-mob").addEventListener("click", () => deletePayment(p.id));
    mobileList.appendChild(mCard);
  });
}

// DIALOGS & FORMS MODALS CONTROLLER
function openModal(modalId) {
  const modal = document.getElementById(modalId);
  if (modal) {
    modal.classList.add("active");
  }
}

function closeModal(modalId) {
  const modal = document.getElementById(modalId);
  if (modal) {
    modal.classList.remove("active");
  }
}

// 1. ADD DEBTOR MODAL
function openAddDebtorModal() {
  document.getElementById("form-debtor").reset();
  document.getElementById("debtor-form-id").value = "";
  document.getElementById("modal-debtor-title").textContent = i18n[appState.currentLang].modal_add_debtor_title;
  openModal("modal-debtor");
}

// Helper to get 1 month later date
function getMonthLaterDateString(dateStr) {
  const d = new Date(dateStr);
  d.setMonth(d.getMonth() + 1);
  const yyyy = d.getFullYear();
  let mm = d.getMonth() + 1;
  let dd = d.getDate();
  if (dd < 10) dd = "0" + dd;
  if (mm < 10) mm = "0" + mm;
  return `${yyyy}-${mm}-${dd}`;
}

// 2. ADD/EDIT DEBT MODAL
function openAddDebtModal(preselectedDebtorId = null) {
  document.getElementById("form-debt").reset();
  document.getElementById("debt-form-id").value = "";
  document.getElementById("modal-debt-title").textContent = i18n[appState.currentLang].modal_add_debt_title;
  
  // Hide all custom inputs
  document.querySelectorAll(".item-custom-text").forEach(inp => {
    inp.style.display = "none";
    inp.removeAttribute("required");
  });

  // Set current date & default due date (1 month later)
  const today = getTodayDateString();
  document.getElementById("debt-date").value = today;
  document.getElementById("debt-due-date").value = getMonthLaterDateString(today);
  document.getElementById("debt-total-amount").value = "0 ៛";

  if (preselectedDebtorId) {
    const debtor = appState.debtors.find(d => d.id === preselectedDebtorId);
    if (debtor) {
      document.getElementById("debt-debtor-name").value = debtor.name;
      document.getElementById("debt-debtor-phone").value = debtor.phone || "";
      document.getElementById("debt-village").value = debtor.address || "";
    }
  } else {
    document.getElementById("debt-debtor-name").value = "";
    document.getElementById("debt-debtor-phone").value = "";
    document.getElementById("debt-village").value = "";
  }
  
  updateDebtRowsVisibility();
  openModal("modal-debt");
}

function openEditDebtModal(debtId) {
  const debt = appState.debts.find(d => d.id === debtId);
  if (!debt) return;

  document.getElementById("debt-form-id").value = debt.id;
  
  const debtor = appState.debtors.find(dr => dr.id === debt.debtorId);
  document.getElementById("debt-debtor-name").value = debtor ? debtor.name : "";
  document.getElementById("debt-debtor-phone").value = debtor ? (debtor.phone || "") : "";
  document.getElementById("debt-village").value = debt.village || (debtor ? debtor.address : "");
  document.getElementById("debt-date").value = debt.date;
  document.getElementById("debt-due-date").value = debt.dueDate || "";
  
  // Set items rows
  const rows = document.querySelectorAll(".debt-item-row");
  rows.forEach((row, idx) => {
    const select = row.querySelector(".item-select");
    const customText = row.querySelector(".item-custom-text");
    const priceInp = row.querySelector(".item-price");
    const qtyInp = row.querySelector(".item-qty");
    
    const itemData = debt.items && debt.items[idx];
    
    if (itemData) {
      const isPredefined = ["ខោទឹកនោម", "ទឹកដោះគោ", "សាច់ជ្រូក", "ចំណីរជ្រូក", "ទឹកសុទ្ធ", "អង្ករ", "គ្រឿងក្រអូប", "សម្ភារៈក្មេង", "ចងការលុយ", "ទឹកផ្លែឈើ", "ស្រាបៀ", "បារី"].includes(itemData.name);
      if (isPredefined) {
        select.value = itemData.name;
        customText.style.display = "none";
        customText.value = "";
        customText.removeAttribute("required");
      } else {
        select.value = "Other";
        customText.style.display = "block";
        customText.value = itemData.name;
        customText.setAttribute("required", "true");
      }
      priceInp.value = itemData.price;
      qtyInp.value = itemData.qty;
      priceInp.setAttribute("required", "true");
      qtyInp.setAttribute("required", "true");
    } else {
      select.value = "";
      customText.style.display = "none";
      customText.value = "";
      customText.removeAttribute("required");
      priceInp.value = "";
      qtyInp.value = "";
      priceInp.removeAttribute("required");
      qtyInp.removeAttribute("required");
    }
  });

  calculateFormTotal();
  updateDebtRowsVisibility();

  document.getElementById("modal-debt-title").textContent = i18n[appState.currentLang].action_edit;

  openModal("modal-debt");
}

// 3. PAYMENT MODAL
function openPaymentModal(debtId) {
  const debt = appState.debts.find(d => d.id === debtId);
  if (!debt) return;

  const debtor = appState.debtors.find(dr => dr.id === debt.debtorId);

  document.getElementById("payment-debt-id").value = debt.id;
  document.getElementById("payment-debtor-id").value = debt.debtorId;
  
  document.getElementById("payment-info-name").textContent = debtor ? debtor.name : "---";
  document.getElementById("payment-info-item").textContent = debt.item;
  document.getElementById("payment-info-remaining").textContent = formatCurrency(debt.remaining, debt.currency);
  
  document.getElementById("payment-amount").value = debt.remaining;
  document.getElementById("payment-amount").setAttribute("max", debt.remaining);
  document.getElementById("payment-currency").value = debt.currency;
  document.getElementById("payment-date").value = getTodayDateString();
  document.getElementById("payment-notes").value = getCurrentTimeFormatted(appState.currentLang);

  openModal("modal-payment");
}

// 4. DEBTOR DETAILS / CUSTOMER FILE MODAL
function openDebtorDetailModal(debtorId) {
  const debtor = appState.debtors.find(d => d.id === debtorId);
  if (!debtor) return;

  // Set Profile details
  document.getElementById("detail-profile-initial").textContent = debtor.name.trim().charAt(0);
  document.getElementById("detail-profile-name").textContent = debtor.name;
  document.getElementById("detail-profile-phone").textContent = `${i18n[appState.currentLang].lbl_debtor_phone}៖ ${debtor.phone || "---"}`;
  document.getElementById("detail-profile-address").textContent = `${i18n[appState.currentLang].lbl_debtor_address}៖ ${debtor.address || "---"}`;

  // Store active ID on modal actions for reference
  document.getElementById("detail-action-add-debt").dataset.debtorId = debtorId;
  document.getElementById("detail-action-pay-total").dataset.debtorId = debtorId;
  document.getElementById("detail-action-share").dataset.debtorId = debtorId;
  document.getElementById("detail-action-print").dataset.debtorId = debtorId;
  document.getElementById("detail-action-edit-profile").dataset.debtorId = debtorId;

  // Load detailed lists
  renderDebtorDetailSublists(debtorId);

  openModal("modal-debtor-detail");
}

function renderDebtorDetailSublists(debtorId) {
  let totalUSD = 0;
  let totalKHR = 0;

  // 1. Debts Sublist
  const debtsBody = document.getElementById("detail-debts-tbody");
  debtsBody.innerHTML = "";

  const activeDebts = appState.debts.filter(d => d.debtorId === debtorId && d.remaining > 0);

  if (activeDebts.length === 0) {
    debtsBody.innerHTML = `<tr><td colspan="7" class="text-center text-muted">${i18n[appState.currentLang].no_debts_found}</td></tr>`;
  } else {
    activeDebts.forEach(d => {
      if (d.currency === "USD") totalUSD += d.remaining;
      else totalKHR += d.remaining;

      const tr = document.createElement("tr");
      tr.innerHTML = `
        <td>${d.item}</td>
        <td class="val-amount">${formatCurrency(d.amount, d.currency)}</td>
        <td class="val-amount text-danger">${formatCurrency(d.remaining, d.currency)}</td>
        <td class="val-date">${d.date}</td>
        <td class="val-date">${d.dueDate || "---"}</td>
        <td><span class="badge badge-${d.status}">${i18n[appState.currentLang][`status_${d.status}`]}</span></td>
        <td class="text-right">
          <button class="btn btn-success btn-sm btn-pay-detail" data-id="${d.id}">${i18n[appState.currentLang].action_pay}</button>
        </td>
      `;
      tr.querySelector(".btn-pay-detail").addEventListener("click", () => {
        closeModal("modal-debtor-detail");
        openPaymentModal(d.id);
      });
      debtsBody.appendChild(tr);
    });
  }

  // Write balance summary to header card
  document.getElementById("detail-balance-usd").textContent = formatCurrency(totalUSD, "USD");
  document.getElementById("detail-balance-khr").textContent = formatCurrency(totalKHR, "KHR");

  // 2. Payments Sublist
  const paymentsBody = document.getElementById("detail-payments-tbody");
  paymentsBody.innerHTML = "";

  const debtorPayments = appState.payments.filter(p => p.debtorId === debtorId);

  if (debtorPayments.length === 0) {
    paymentsBody.innerHTML = `<tr><td colspan="6" class="text-center text-muted">${i18n[appState.currentLang].no_payments_found}</td></tr>`;
  } else {
    debtorPayments.forEach(p => {
      const debt = appState.debts.find(d => d.id === p.debtId);
      const currency = debt ? debt.currency : "USD";
      const itemDesc = debt ? debt.item : "---";

      const tr = document.createElement("tr");
      tr.innerHTML = `
        <td>${itemDesc}</td>
        <td class="val-amount text-emerald">${formatCurrency(p.amount, currency)}</td>
        <td class="val-date">${p.date}</td>
        <td><span class="badge badge-paid">${p.method}</span></td>
        <td><span class="text-secondary">${p.notes || "---"}</span></td>
        <td class="text-right">
          <button class="btn btn-outline btn-sm border-danger text-danger btn-delete-pay-detail" data-id="${p.id}">🗑️</button>
        </td>
      `;
      
      tr.querySelector(".btn-delete-pay-detail").addEventListener("click", () => {
        closeModal("modal-debtor-detail");
        deletePayment(p.id);
      });
      
      paymentsBody.appendChild(tr);
    });
  }
}

// FORM DROPDOWNS POPULATER
function populateDebtorDropdowns() {
  // Stub function to maintain compatibility if called elsewhere
}

function calculateFormTotal() {
  let total = 0;
  const rows = document.querySelectorAll(".debt-item-row");
  
  rows.forEach(row => {
    const select = row.querySelector(".item-select");
    const priceInp = row.querySelector(".item-price");
    const qtyInp = row.querySelector(".item-qty");
    
    if (select && select.value !== "") {
      const price = parseFloat(priceInp.value) || 0;
      const qty = parseFloat(qtyInp.value) || 0;
      total += price * qty;
    }
  });

  const totalInput = document.getElementById("debt-total-amount");
  if (totalInput) {
    totalInput.value = total.toLocaleString() + " ៛";
  }
}

function updateDebtRowsVisibility() {
  const rows = document.querySelectorAll(".debt-item-row");
  if (rows.length === 0) return;

  const hasAnyInput = (row) => {
    const select = row.querySelector(".item-select");
    const customText = row.querySelector(".item-custom-text");
    const priceInp = row.querySelector(".item-price");
    const qtyInp = row.querySelector(".item-qty");

    const hasItemSelect = select && select.value !== "";
    const hasCustomText = customText && customText.value.trim() !== "";
    const hasPrice = priceInp && priceInp.value.trim() !== "";
    const hasQty = qtyInp && qtyInp.value.trim() !== "";

    return hasItemSelect || hasCustomText || hasPrice || hasQty;
  };

  const isCompletelyFilled = (row) => {
    const select = row.querySelector(".item-select");
    const customText = row.querySelector(".item-custom-text");
    const priceInp = row.querySelector(".item-price");
    const qtyInp = row.querySelector(".item-qty");

    const hasItemSelect = select && select.value !== "";
    const hasPrice = priceInp && priceInp.value.trim() !== "";
    const hasQty = qtyInp && qtyInp.value.trim() !== "";

    let isCustomValid = true;
    if (select && select.value === "Other") {
      isCustomValid = customText && customText.value.trim() !== "";
    }

    return hasItemSelect && hasPrice && hasQty && isCustomValid;
  };

  // Row 1 is always visible
  rows[0].style.display = "";

  // For subsequent rows, show if the previous row is visible and completely filled, or if the current row has some input (e.g. edit mode)
  for (let i = 1; i < rows.length; i++) {
    const prevRowVisible = rows[i - 1].style.display !== "none";
    const prevRowCompletelyFilled = isCompletelyFilled(rows[i - 1]);
    const currentHasInput = hasAnyInput(rows[i]);

    const shouldShow = (prevRowVisible && prevRowCompletelyFilled) || currentHasInput;
    rows[i].style.display = shouldShow ? "" : "none";
  }
}

// OPERATIONS: ADD/EDIT/DELETE SUBMISSIONS

// 1. Save Debtor
function saveDebtor(e) {
  e.preventDefault();
  
  const idInput = document.getElementById("debtor-form-id").value;
  const name = document.getElementById("debtor-name").value.trim();
  const phone = document.getElementById("debtor-phone").value.trim();
  const address = document.getElementById("debtor-address").value.trim();

  if (idInput) {
    // Edit Mode
    const index = appState.debtors.findIndex(d => d.id === idInput);
    if (index !== -1) {
      appState.debtors[index] = { ...appState.debtors[index], name, phone, address };
    }
  } else {
    // Add Mode
    const newId = "debtor_" + Date.now();
    appState.debtors.push({ id: newId, name, phone, address });
  }

  saveToLocalStorage();
  closeModal("modal-debtor");
  showToast(i18n[appState.currentLang].toast_saved);
  
  renderCurrentView();
}

// 2. Save Debt Record
function saveDebt(e) {
  e.preventDefault();

  const idInput = document.getElementById("debt-form-id").value;
  const typedName = document.getElementById("debt-debtor-name").value.trim();
  const typedPhone = document.getElementById("debt-debtor-phone").value.trim();
  const selectedVillage = document.getElementById("debt-village").value;
  const date = document.getElementById("debt-date").value;
  const dueDate = document.getElementById("debt-due-date").value || null;

  if (!typedName || !selectedVillage) {
    alert("សូមបញ្ចូលឈ្មោះអ្នកជំពាក់ និងជ្រើសរើសភូមិ!");
    return;
  }

  // 1. Resolve or create Debtor
  let debtor = appState.debtors.find(d => d.name.toLowerCase() === typedName.toLowerCase());
  if (!debtor) {
    const newDebtorId = "debtor_" + Date.now();
    debtor = { id: newDebtorId, name: typedName, phone: typedPhone, address: selectedVillage };
    appState.debtors.push(debtor);
  } else {
    debtor.phone = typedPhone;
    debtor.address = selectedVillage;
  }

  // 2. Extract items from rows with JS Validation
  const items = [];
  const rows = document.querySelectorAll(".debt-item-row");
  let validationError = false;

  rows.forEach((row, idx) => {
    if (validationError) return;

    const select = row.querySelector(".item-select");
    const customText = row.querySelector(".item-custom-text");
    const priceInp = row.querySelector(".item-price");
    const qtyInp = row.querySelector(".item-qty");

    const hasItemSelect = select && select.value !== "";
    const hasPrice = priceInp && priceInp.value.trim() !== "";
    const hasQty = qtyInp && qtyInp.value.trim() !== "";

    // If they filled out any field in a row, they must fill out the complete row
    if (hasItemSelect || hasPrice || hasQty) {
      if (!hasItemSelect) {
        alert(`សូមជ្រើសរើសប្រភេទអីវ៉ាន់នៅជួរទី ${idx + 1}!`);
        select.focus();
        validationError = true;
        return;
      }
      
      const name = select.value === "Other" ? customText.value.trim() : select.value;
      if (select.value === "Other" && !name) {
        alert(`សូមបញ្ចូលឈ្មោះទំនិញផ្សេងៗនៅជួរទី ${idx + 1}!`);
        customText.focus();
        validationError = true;
        return;
      }

      const price = parseFloat(priceInp.value);
      if (isNaN(price) || price < 0) {
        alert(`សូមបញ្ចូលតម្លៃរាយឱ្យបានត្រឹមត្រូវនៅជួរទី ${idx + 1}!`);
        priceInp.focus();
        validationError = true;
        return;
      }

      const qty = parseFloat(qtyInp.value);
      if (isNaN(qty) || qty <= 0) {
        alert(`សូមបញ្ចូលចំនួនឱ្យបានត្រឹមត្រូវនៅជួរទី ${idx + 1}!`);
        qtyInp.focus();
        validationError = true;
        return;
      }

      items.push({ name, price, qty });
    }
  });

  if (validationError) return;

  if (items.length === 0) {
    alert("សូមបំពេញទិន្នន័យទំនិញជំពាក់ឱ្យបានយ៉ាងហោចណាស់ ១ជួរ!");
    return;
  }

  // Calculate total sum
  const amount = items.reduce((sum, item) => sum + (item.price * item.qty), 0);
  const displayItemStr = items.map(it => `${it.name} (x${it.qty})`).join(", ");

  if (idInput) {
    // Edit existing debt
    const index = appState.debts.findIndex(d => d.id === idInput);
    if (index !== -1) {
      const prevDebt = appState.debts[index];
      let totalPaid = prevDebt.amount - prevDebt.remaining;
      let newRemaining = amount - totalPaid;
      let newStatus = "unpaid";
      
      if (newRemaining <= 0) {
        newRemaining = 0;
        newStatus = "paid";
      } else if (newRemaining < amount) {
        newStatus = "partial";
      }

      appState.debts[index] = {
        ...prevDebt,
        debtorId: debtor.id,
        village: selectedVillage,
        items,
        item: displayItemStr,
        amount,
        remaining: newRemaining,
        status: newStatus,
        date,
        dueDate
      };
    }
  } else {
    // Create new debt
    const newId = "debt_" + Date.now();
    appState.debts.push({
      id: newId,
      debtorId: debtor.id,
      village: selectedVillage,
      items,
      item: displayItemStr,
      amount,
      currency: "KHR",
      date,
      dueDate,
      remaining: amount,
      status: "unpaid",
      notes: ""
    });
  }

  saveToLocalStorage();
  closeModal("modal-debt");
  showToast(i18n[appState.currentLang].toast_saved);
  
  renderCurrentView();
}

// 3. Delete Debt
function deleteDebt(id) {
  // Confirm before deleting
  const lang = appState.currentLang;
  if (!confirm(i18n[lang].confirm_reset.replace("all data", "this debt record"))) return;

  // Remove payments linked to this debt
  appState.payments = appState.payments.filter(p => p.debtId !== id);
  
  // Remove debt
  appState.debts = appState.debts.filter(d => d.id !== id);

  saveToLocalStorage();
  showToast(i18n[lang].toast_deleted);
  
  renderCurrentView();
}

// 4. Save Payment
function recordPayment(e) {
  e.preventDefault();

  const debtId = document.getElementById("payment-debt-id").value;
  const debtorId = document.getElementById("payment-debtor-id").value;
  const amount = parseFloat(document.getElementById("payment-amount").value);
  const date = document.getElementById("payment-date").value;
  const method = document.getElementById("payment-method").value;
  const notes = document.getElementById("payment-notes").value.trim();

  const debtIndex = appState.debts.findIndex(d => d.id === debtId);
  if (debtIndex === -1) return;

  const debt = appState.debts[debtIndex];
  
  // Validation checks
  if (amount <= 0 || amount > debt.remaining) {
    alert("Invalid payment amount!");
    return;
  }

  // Deduct from debt remaining
  const newRemaining = parseFloat((debt.remaining - amount).toFixed(4));
  let status = "partial";
  if (newRemaining <= 0) {
    status = "paid";
  }

  appState.debts[debtIndex].remaining = newRemaining;
  appState.debts[debtIndex].status = status;

  // Add repayment transaction log
  const paymentId = "pay_" + Date.now();
  appState.payments.push({
    id: paymentId,
    debtId,
    debtorId,
    amount,
    date,
    method,
    notes
  });

  saveToLocalStorage();
  closeModal("modal-payment");
  showToast(i18n[appState.currentLang].toast_payment_added);
  
  renderCurrentView();
}

// 4.5. ADD AGGREGATE PAYMENT MODAL & CONFIRMATION
function openAggregatePaymentModal(debtorId) {
  const debtor = appState.debtors.find(d => d.id === debtorId);
  if (!debtor) return;

  // Calculate balances
  let totalUSD = 0;
  let totalKHR = 0;
  appState.debts.forEach(d => {
    if (d.debtorId === debtorId && d.remaining > 0) {
      if (d.currency === "USD") totalUSD += d.remaining;
      else totalKHR += d.remaining;
    }
  });

  document.getElementById("aggregate-payment-debtor-id").value = debtorId;
  document.getElementById("aggregate-payment-info-name").textContent = debtor.name;
  document.getElementById("aggregate-payment-info-usd").textContent = formatCurrency(totalUSD, "USD");
  document.getElementById("aggregate-payment-info-khr").textContent = formatCurrency(totalKHR, "KHR");

  // Default values
  document.getElementById("aggregate-payment-amount").value = totalKHR > 0 ? totalKHR : (totalUSD > 0 ? totalUSD : 0);
  document.getElementById("aggregate-payment-currency").value = totalKHR > 0 ? "KHR" : "USD";
  document.getElementById("aggregate-payment-date").value = getTodayDateString();
  document.getElementById("aggregate-payment-notes").value = getCurrentTimeFormatted(appState.currentLang);

  // Set constraint
  const updateLimit = () => {
    const curr = document.getElementById("aggregate-payment-currency").value;
    const limit = curr === "USD" ? totalUSD : totalKHR;
    document.getElementById("aggregate-payment-amount").setAttribute("max", limit);
    document.getElementById("aggregate-payment-amount").value = limit;
  };
  
  document.getElementById("aggregate-payment-currency").onchange = updateLimit;
  updateLimit();

  openModal("modal-aggregate-payment");
}

function saveAggregatePayment(e) {
  e.preventDefault();

  const debtorId = document.getElementById("aggregate-payment-debtor-id").value;
  const currency = document.getElementById("aggregate-payment-currency").value;
  const amount = parseFloat(document.getElementById("aggregate-payment-amount").value);
  const date = document.getElementById("aggregate-payment-date").value;
  const method = document.getElementById("aggregate-payment-method").value;
  const notes = document.getElementById("aggregate-payment-notes").value.trim();

  if (amount <= 0) {
    alert("សូមបញ្ចូលចំនួនទឹកប្រាក់ទូទាត់ឱ្យបានត្រឹមត្រូវ!");
    return;
  }

  // Get active matching currency debts sorted by date (FIFO - oldest first)
  const matchingDebts = appState.debts.filter(d => d.debtorId === debtorId && d.currency === currency && d.remaining > 0);
  matchingDebts.sort((a, b) => new Date(a.date) - new Date(b.date));

  // Verify total debt check
  const totalDebtAvailable = matchingDebts.reduce((sum, d) => sum + d.remaining, 0);
  if (amount > totalDebtAvailable) {
    alert("ទឹកប្រាក់ទូទាត់មិនអាចលើសពីសមតុល្យជំពាក់សរុបឡើយ!");
    return;
  }

  let remainingToDistribute = amount;

  matchingDebts.forEach(d => {
    if (remainingToDistribute <= 0) return;

    const debtIndex = appState.debts.findIndex(debt => debt.id === d.id);
    if (debtIndex === -1) return;

    const currentDebt = appState.debts[debtIndex];
    const canPay = parseFloat(Math.min(currentDebt.remaining, remainingToDistribute).toFixed(4));
    
    // deduct from debt
    const newRemaining = parseFloat((currentDebt.remaining - canPay).toFixed(4));
    appState.debts[debtIndex].remaining = newRemaining;
    appState.debts[debtIndex].status = newRemaining <= 0 ? "paid" : "partial";

    // log payment transaction
    const payId = "pay_" + Date.now() + "_" + Math.random().toString(36).substr(2, 5);
    appState.payments.push({
      id: payId,
      debtId: currentDebt.id,
      debtorId,
      amount: canPay,
      date,
      method,
      notes: notes || (appState.currentLang === "km" ? "សងកាត់បំណុលរួម" : "Aggregate payment")
    });

    remainingToDistribute = parseFloat((remainingToDistribute - canPay).toFixed(4));
  });

  saveToLocalStorage();
  closeModal("modal-aggregate-payment");
  showToast(i18n[appState.currentLang].toast_payment_added);

  // If debtor modal details is open, refresh it
  const detailModal = document.getElementById("modal-debtor-detail");
  if (detailModal && detailModal.classList.contains("active")) {
    renderDebtorDetailSublists(debtorId);
  }

  renderCurrentView();
}

// 5. Delete Repayment
function deletePayment(id) {
  const payment = appState.payments.find(p => p.id === id);
  if (!payment) return;

  // Add amount back to debt remaining balance
  const debtIndex = appState.debts.findIndex(d => d.id === payment.debtId);
  if (debtIndex !== -1) {
    const debt = appState.debts[debtIndex];
    const newRemaining = debt.remaining + payment.amount;
    let status = "partial";
    if (newRemaining >= debt.amount) {
      status = "unpaid";
    }
    appState.debts[debtIndex].remaining = newRemaining;
    appState.debts[debtIndex].status = status;
  }

  // Remove payment
  appState.payments = appState.payments.filter(p => p.id !== id);

  saveToLocalStorage();
  showToast(i18n[appState.currentLang].toast_deleted);
  
  renderCurrentView();
}

// 6.5. Print Debtor Statement
function printDebtorStatement(debtorId) {
  const debtor = appState.debtors.find(d => d.id === debtorId);
  if (!debtor) return;

  const activeDebts = appState.debts.filter(d => d.debtorId === debtorId && d.status !== "paid" && d.remaining > 0);
  
  let totalUSD = 0;
  let totalKHR = 0;
  
  let rowsHtml = "";
  let itemIndex = 1;

  if (activeDebts.length === 0) {
    rowsHtml = `<tr><td colspan="7" style="text-align: center; padding: 20px; color: #666;">មិនមានបំណុលមិនទាន់ទូទាត់ឡើយ</td></tr>`;
  } else {
    activeDebts.forEach(d => {
      if (d.currency === "USD") totalUSD += d.remaining;
      else totalKHR += d.remaining;
      
      rowsHtml += `
        <tr>
          <td style="text-align: center;">${itemIndex++}</td>
          <td style="font-size: 0.8rem; white-space: nowrap;">${d.date}</td>
          <td style="font-weight: 500;">${d.item}</td>
          <td style="text-align: right; font-family: 'Inter', sans-serif;">${formatCurrency(d.remaining, d.currency)}</td>
          <td style="text-align: center; font-family: 'Inter', sans-serif;">1</td>
          <td style="text-align: right; font-family: 'Inter', sans-serif; font-weight: 600;">${formatCurrency(d.remaining, d.currency)}</td>
          <td style="text-align: right; font-family: 'Inter', sans-serif; font-weight: 700; color: #d9534f;">${formatCurrency(d.remaining, d.currency)}</td>
        </tr>
      `;
    });
  }

  const printArea = document.getElementById("print-area");
  if (!printArea) return;

  printArea.innerHTML = `
    <div class="print-header">
      <div class="print-title">របាយការណ៍ជំពាក់ប្រាក់</div>
      <div class="print-shop-name" style="font-weight: bold;">ហាង សុខ ស៊ីណេត (ទូរសព្ទ៖ 097 500 3990)</div>
      <div class="print-shop-address" style="font-size: 0.75rem; color: #444; margin-top: 3px; font-family: 'Khmer OS Siemreap', 'Siemreap', sans-serif;">អាសយដ្ឋាន៖ កែងផ្លូវទៅច្រេស ភូមិព្រៃម្នាស់ ឃុំសំឡី ស្រុកកំពង់រោទិ៍ ខេត្តស្វាយរៀង។</div>
      <div class="print-divider"></div>
    </div>
    
    <div class="print-meta-grid">
      <div class="print-meta-section">
        <p><strong>អតិថិជន (Debtor)៖</strong> ${debtor.name}</p>
        <p><strong>លេខទូរសព្ទ (Phone)៖</strong> ${debtor.phone || "---"}</p>
        <p><strong>អាសយដ្ឋាន (Village)៖</strong> ${debtor.address || "---"}</p>
      </div>
      <div class="print-meta-section" style="text-align: right;">
        <p><strong>ថ្ងៃបោះពុម្ព (Date)៖</strong> ${getTodayDateString()}</p>
        <p><strong>ស្ថានភាព (Status)៖</strong> ${activeDebts.length === 0 ? "ទូទាត់រួចរាល់" : "មិនទាន់ទូទាត់រួចរាល់"}</p>
      </div>
    </div>
    
    <table class="print-table">
      <thead>
        <tr>
          <th style="width: 5%; text-align: center;">ល.រ</th>
          <th style="width: 15%;">កាលបរិច្ឆេទ</th>
          <th style="width: 35%;">មុខទំនិញ / ការពិពណ៌នា</th>
          <th style="width: 15%; text-align: right;">តម្លៃរាយ</th>
          <th style="width: 8%; text-align: center;">ចំនួន</th>
          <th style="width: 12%; text-align: right;">សរុប</th>
          <th style="width: 15%; text-align: right;">នៅខ្វះ</th>
        </tr>
      </thead>
      <tbody>
        ${rowsHtml}
      </tbody>
    </table>
    
    <div class="print-summary-container">
      <div class="print-summary-box">
        <div class="print-summary-row">
          <span>ជំពាក់ USD សរុប៖</span>
          <span style="font-family: 'Inter', sans-serif; font-weight: bold; color: #555;">${formatCurrency(totalUSD, "USD")}</span>
        </div>
        <div class="print-summary-row">
          <span>ជំពាក់ KHR សរុប៖</span>
          <span style="font-family: 'Inter', sans-serif; font-weight: bold; color: #555;">${formatCurrency(totalKHR, "KHR")}</span>
        </div>
        <div class="print-summary-row total">
          <span>សរុបត្រូវសង (Total)៖</span>
          <span style="font-family: 'Inter', sans-serif;">
            ${totalKHR > 0 ? formatCurrency(totalKHR, "KHR") : ""}${totalKHR > 0 && totalUSD > 0 ? " / " : ""}${totalUSD > 0 ? formatCurrency(totalUSD, "USD") : totalKHR === 0 ? "0 ៛" : ""}
          </span>
        </div>
      </div>
    </div>
    
    <div class="print-footer-notice">
      <p><strong>គោលការណ៍ហាង៖</strong> អតិថិជនទាំងអស់ត្រូវមកទូទាត់សំណងជារៀងរាល់ថ្ងៃក្នុងរយៈពេល១ខែ។</p>
      <p style="margin-top: 6px; font-weight: bold; color: #111;">សូមអរគុណសម្រាប់ការគាំទ្រហាងយើងខ្ញុំ! (Thank you for your business!)</p>
    </div>
    
    <div class="print-signatures">
      <div class="print-sig-col">
        <p>ស្នាមមេដៃ/ហត្ថលេខាអតិថិជន</p>
        <div class="print-sig-line">អតិថិជន / Customer</div>
      </div>
      <div class="print-sig-col">
        <p>ហត្ថលេខាម្ចាស់ហាង</p>
        <div class="print-sig-line">ម្ចាស់ហាង / Shop Owner</div>
      </div>
    </div>
  `;

  // Trigger print dialog
  window.print();
}

// 6.6. Print All Unpaid Debts Grouped By Debtor Name
function printAllUnpaidDebtsGrouped() {
  const activeDebts = appState.debts.filter(d => d.status !== "paid" && d.remaining > 0);
  
  if (activeDebts.length === 0) {
    alert(appState.currentLang === "km" ? "មិនមានបំណុលដែលមិនទាន់ទូទាត់ឡើយ" : "No outstanding debts found.");
    return;
  }
  
  // Group debts by debtorId
  const debtsByDebtor = {};
  activeDebts.forEach(d => {
    if (!debtsByDebtor[d.debtorId]) {
      debtsByDebtor[d.debtorId] = [];
    }
    debtsByDebtor[d.debtorId].push(d);
  });
  
  let overallTotalUSD = 0;
  let overallTotalKHR = 0;
  
  let groupsHtml = "";
  
  // Sort debtors alphabetically by name
  const debtorIds = Object.keys(debtsByDebtor);
  const debtorsList = debtorIds.map(id => {
    return {
      debtor: appState.debtors.find(debtor => debtor.id === id) || { id, name: "Unknown Customer", phone: "", address: "" },
      debts: debtsByDebtor[id]
    };
  });
  
  debtorsList.sort((a, b) => a.debtor.name.localeCompare(b.debtor.name, 'km'));
  
  debtorsList.forEach(({ debtor, debts }) => {
    let debtorTotalUSD = 0;
    let debtorTotalKHR = 0;
    let rowsHtml = "";
    
    debts.forEach((d, idx) => {
      if (d.currency === "USD") {
        debtorTotalUSD += d.remaining;
        overallTotalUSD += d.remaining;
      } else {
        debtorTotalKHR += d.remaining;
        overallTotalKHR += d.remaining;
      }
      
      rowsHtml += `
        <tr>
          <td style="text-align: center;">${idx + 1}</td>
          <td style="font-size: 0.8rem; white-space: nowrap;">${d.date}</td>
          <td style="font-weight: 500;">${d.item}</td>
          <td style="text-align: right; font-family: 'Inter', sans-serif;">${formatCurrency(d.remaining, d.currency)}</td>
          <td style="text-align: center; font-family: 'Inter', sans-serif;">1</td>
          <td style="text-align: right; font-family: 'Inter', sans-serif; font-weight: 600;">${formatCurrency(d.remaining, d.currency)}</td>
          <td style="text-align: right; font-family: 'Inter', sans-serif; font-weight: 700; color: #d9534f;">${formatCurrency(d.remaining, d.currency)}</td>
        </tr>
      `;
    });
    
    groupsHtml += `
      <div class="print-debtor-group">
        <div class="print-debtor-group-header">
          <div class="print-debtor-group-title">👤 ${debtor.name}</div>
          <div class="print-debtor-group-meta">
            ${debtor.phone ? `📞 ទូរសព្ទ៖ ${debtor.phone}` : ""}
            ${debtor.address ? ` 📍 ភូមិ៖ ${debtor.address}` : ""}
          </div>
        </div>
        <table class="print-table" style="margin-bottom: 8px;">
          <thead>
            <tr>
              <th style="width: 5%; text-align: center;">ល.រ</th>
              <th style="width: 15%;">កាលបរិច្ឆេទ</th>
              <th style="width: 35%;">មុខទំនិញ / ការពិពណ៌នា</th>
              <th style="width: 15%; text-align: right;">តម្លៃរាយ</th>
              <th style="width: 8%; text-align: center;">ចំនួន</th>
              <th style="width: 12%; text-align: right;">សរុប</th>
              <th style="width: 15%; text-align: right;">នៅខ្វះ</th>
            </tr>
          </thead>
          <tbody>
            ${rowsHtml}
          </tbody>
        </table>
        <div style="text-align: right; font-size: 0.8rem; font-weight: bold; color: #333; margin-top: 4px; margin-bottom: 8px;">
          ជំពាក់សរុប៖ 
          ${debtorTotalKHR > 0 ? formatCurrency(debtorTotalKHR, "KHR") : ""}${debtorTotalKHR > 0 && debtorTotalUSD > 0 ? " / " : ""}${debtorTotalUSD > 0 ? formatCurrency(debtorTotalUSD, "USD") : debtorTotalKHR === 0 ? "0 ៛" : ""}
        </div>
      </div>
    `;
  });
  
  const printArea = document.getElementById("print-area");
  if (!printArea) return;
  
  printArea.className = "multi-page-print";
  printArea.innerHTML = `
    <div class="print-header" style="margin-bottom: 20px;">
      <div class="print-title">របាយការណ៍សរុបបំណុលមិនទាន់ទូទាត់</div>
      <div class="print-shop-name" style="font-weight: bold;">ហាង សុខ ស៊ីណេត (ទូរសព្ទ៖ 097 500 3990)</div>
      <div class="print-shop-address" style="font-size: 0.75rem; color: #444; margin-top: 3px; font-family: 'Khmer OS Siemreap', 'Siemreap', sans-serif;">អាសយដ្ឋាន៖ កែងផ្លូវទៅច្រេស ភូមិព្រៃម្នាស់ ឃុំសំឡី ស្រុកកំពង់រោទិ៍ ខេត្តស្វាយរៀង។</div>
      <div style="font-size: 0.75rem; color: #555; margin-top: 4px;">ថ្ងៃបោះពុម្ព៖ ${getTodayDateString()}</div>
      <div class="print-divider" style="margin-bottom: 12px;"></div>
    </div>

    <div style="background-color: #f8f9fa; border: 1.5px solid #222; border-radius: 6px; padding: 12px; margin-bottom: 20px; font-size: 0.85rem;">
      <div style="font-weight: bold; font-size: 0.95rem; margin-bottom: 6px; border-bottom: 1px solid #ddd; padding-bottom: 4px; font-family: 'Khmer OS Muol Light', 'Khmer OS Muol', 'Moul', sans-serif;">សេចក្តីសង្ខេបរួម (Grand Summary)</div>
      <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 8px;">
        <div><strong>ចំនួនអតិថិជនកំពុងជំពាក់៖</strong> ${debtorsList.length} នាក់</div>
        <div style="text-align: right;"><strong>សរុបទឹកប្រាក់ត្រូវទារ៖</strong> 
          <span style="color: #d9534f; font-weight: bold;">
            ${overallTotalKHR > 0 ? formatCurrency(overallTotalKHR, "KHR") : ""}${overallTotalKHR > 0 && overallTotalUSD > 0 ? " / " : ""}${overallTotalUSD > 0 ? formatCurrency(overallTotalUSD, "USD") : overallTotalKHR === 0 ? "0 ៛" : ""}
          </span>
        </div>
      </div>
    </div>
    
    <div class="print-groups-container">
      ${groupsHtml}
    </div>
    
    <div class="print-footer-notice" style="margin-top: 30px;">
      <p style="font-weight: bold; color: #111;">របាយការណ៍បំណុលមិនទាន់ទូទាត់សរុបប្រព័ន្ធ</p>
    </div>
  `;
  
  // Trigger print dialog
  window.print();
  
  // Restore print area class list
  printArea.className = "";
}

// 6. Share Debtor Statement (Format nicely and copy)
function shareDebtorStatement(debtorId) {
  const debtor = appState.debtors.find(d => d.id === debtorId);
  if (!debtor) return;

  const activeDebts = appState.debts.filter(d => d.debtorId === debtorId && d.status !== "paid" && d.remaining > 0);
  
  let totalUSD = 0;
  let totalKHR = 0;
  
  let statementText = "";

  if (appState.currentLang === "km") {
    statementText = `🧾 *របាយការណ៍សង្ខេបសមតុល្យគណនីជំពាក់*\n\n`;
    statementText += `👤 *អតិថិជន៖* ${debtor.name}\n`;
    if (debtor.phone) statementText += `📞 *ទូរសព្ទ៖* ${debtor.phone}\n`;
    statementText += `📅 *កាលបរិច្ឆេទ៖* ${getTodayDateString()}\n\n`;
    statementText += `🛍️ *ព័ត៌មានទំនិញជំពាក់៖*\n`;
    
    if (activeDebts.length === 0) {
      statementText += `- មិនមានបំណុលដែលមិនទាន់ទូទាត់ទេ\n`;
    } else {
      activeDebts.forEach(d => {
        statementText += `- ${d.item}៖ ${formatCurrency(d.amount, d.currency)} (នៅខ្វះ ${formatCurrency(d.remaining, d.currency)})\n`;
        if (d.currency === "USD") totalUSD += d.remaining;
        else totalKHR += d.remaining;
      });
    }
    
    statementText += `\n💵 *ទឹកប្រាក់ត្រូវទូទាត់សរុប៖*\n`;
    statementText += `👉 *ជាដុល្លារ៖* $${totalUSD.toFixed(2)}\n`;
    statementText += `👉 *ជារៀល៖* ${totalKHR.toLocaleString()} ៛\n\n`;
    statementText += `🙏 សូមអរគុណសម្រាប់ការគាំទ្រហាងយើងខ្ញុំ!`;
  } else {
    statementText = `🧾 *Outstanding Balance Statement*\n\n`;
    statementText += `👤 *Customer:* ${debtor.name}\n`;
    if (debtor.phone) statementText += `📞 *Phone:* ${debtor.phone}\n`;
    statementText += `📅 *Date:* ${getTodayDateString()}\n\n`;
    statementText += `🛍️ *Details of Debts:*\n`;
    
    if (activeDebts.length === 0) {
      statementText += `- No outstanding debts\n`;
    } else {
      activeDebts.forEach(d => {
        statementText += `- ${d.item}: ${formatCurrency(d.amount, d.currency)} (Remaining ${formatCurrency(d.remaining, d.currency)})\n`;
        if (d.currency === "USD") totalUSD += d.remaining;
        else totalKHR += d.remaining;
      });
    }
    
    statementText += `\n💵 *Total Outstanding Balance:*\n`;
    statementText += `👉 *USD:* $${totalUSD.toFixed(2)}\n`;
    statementText += `👉 *KHR:* ${totalKHR.toLocaleString()} ៛\n\n`;
    statementText += `🙏 Thank you for your business!`;
  }

  // Copy to clipboard
  navigator.clipboard.writeText(statementText)
    .then(() => showToast(i18n[appState.currentLang].toast_copied))
    .catch(err => console.error("Error copying statement: ", err));
}

// GENERAL EVENT HANDLERS
function initEventHandlers() {
  // Global search input
  const setupSearch = (inputId) => {
    const input = document.getElementById(inputId);
    if (input) {
      input.addEventListener("input", (e) => {
        appState.searchQuery = e.target.value;
        // Sync another search inputs if they exist
        const searches = ["global-search", "debtors-search", "debts-search", "payments-search"];
        searches.forEach(id => {
          const target = document.getElementById(id);
          if (target && target !== input) {
            target.value = e.target.value;
          }
        });
        renderCurrentView();
      });
    }
  };

  setupSearch("global-search");
  setupSearch("debtors-search");
  setupSearch("debts-search");
  setupSearch("payments-search");

  // Filter tabs on Debts
  document.querySelectorAll(".tab-item").forEach(tab => {
    tab.addEventListener("click", () => {
      document.querySelectorAll(".tab-item").forEach(t => t.classList.remove("active"));
      tab.classList.add("active");
      appState.activeFilter = tab.dataset.filter;
      renderCurrentView();
    });
  });

  // Theme quick toggle (header icon)
  const themeToggle = document.getElementById("theme-toggle");
  if (themeToggle) {
    themeToggle.addEventListener("click", () => {
      const nextTheme = appState.currentTheme === "light" ? "dark" : "light";
      setTheme(nextTheme);
    });
  }

  // Language quick toggle (header icon)
  const langToggle = document.getElementById("lang-toggle");
  if (langToggle) {
    langToggle.addEventListener("click", () => {
      const nextLang = appState.currentLang === "km" ? "en" : "km";
      setLanguage(nextLang);
    });
  }

  // Settings page selects
  document.querySelectorAll(".lang-btn-select").forEach(btn => {
    btn.addEventListener("click", () => setLanguage(btn.dataset.lang));
  });

  document.querySelectorAll(".theme-btn-select").forEach(btn => {
    btn.addEventListener("click", () => setTheme(btn.dataset.theme));
  });

  // Modal Closers
  document.querySelectorAll(".btn-close-modal").forEach(btn => {
    btn.addEventListener("click", (e) => {
      const overlay = e.target.closest(".modal-overlay");
      if (overlay) overlay.classList.remove("active");
    });
  });

  document.querySelectorAll(".btn-close-modal-btn").forEach(btn => {
    btn.addEventListener("click", (e) => {
      const overlay = e.target.closest(".modal-overlay");
      if (overlay) overlay.classList.remove("active");
    });
  });

  // Outside click closes modal
  document.querySelectorAll(".modal-overlay").forEach(overlay => {
    overlay.addEventListener("click", (e) => {
      if (e.target === overlay) {
        overlay.classList.remove("active");
      }
    });
  });

  // Quick Action Buttons
  const qAddDebt = document.getElementById("btn-quick-add-debt");
  if (qAddDebt) qAddDebt.addEventListener("click", () => openAddDebtModal());

  const fabAdd = document.getElementById("fab-add-trigger");
  if (fabAdd) fabAdd.addEventListener("click", () => openAddDebtModal());

  const addDebtor = document.getElementById("btn-add-debtor");
  if (addDebtor) addDebtor.addEventListener("click", openAddDebtorModal);

  const addDebtDirect = document.getElementById("btn-add-debt-direct");
  if (addDebtDirect) addDebtDirect.addEventListener("click", () => openAddDebtModal());

  const printAllUnpaidBtn = document.getElementById("btn-print-all-unpaid");
  if (printAllUnpaidBtn) printAllUnpaidBtn.addEventListener("click", printAllUnpaidDebtsGrouped);

  // Forms Submissions hooks
  document.getElementById("form-debtor").addEventListener("submit", saveDebtor);
  document.getElementById("form-debt").addEventListener("submit", saveDebt);
  document.getElementById("form-payment").addEventListener("submit", recordPayment);

  // Auto-update due date when debt date changes
  const debtDateInput = document.getElementById("debt-date");
  if (debtDateInput) {
    debtDateInput.addEventListener("change", (e) => {
      const targetDate = e.target.value;
      if (targetDate) {
        document.getElementById("debt-due-date").value = getMonthLaterDateString(targetDate);
      }
    });
  }

  // Autocomplete Suggestions for Debtor Name
  const debtorNameInput = document.getElementById("debt-debtor-name");
  const suggestionsBox = document.getElementById("debt-debtor-suggestions");

  if (debtorNameInput && suggestionsBox) {
    debtorNameInput.addEventListener("input", (e) => {
      const val = e.target.value.toLowerCase().trim();
      suggestionsBox.innerHTML = "";
      
      if (!val) {
        suggestionsBox.classList.remove("active");
        return;
      }

      const matches = appState.debtors.filter(d => d.name.toLowerCase().includes(val));

      if (matches.length === 0) {
        suggestionsBox.classList.remove("active");
        return;
      }

      suggestionsBox.classList.add("active");
      matches.forEach(m => {
        const item = document.createElement("div");
        item.className = "suggestion-item";
        item.innerHTML = `
          <span class="suggestion-item-name">${m.name}</span>
          <span class="suggestion-item-address" style="font-size:0.75rem;color:var(--text-muted);">${m.address || ""}</span>
        `;
        item.addEventListener("click", () => {
          debtorNameInput.value = m.name;
          document.getElementById("debt-debtor-phone").value = m.phone || "";
          if (m.address) {
            document.getElementById("debt-village").value = m.address;
          }
          suggestionsBox.innerHTML = "";
          suggestionsBox.classList.remove("active");
        });
        suggestionsBox.appendChild(item);
      });
    });

    // Close suggestions box when clicking outside
    document.addEventListener("click", (e) => {
      if (e.target !== debtorNameInput && e.target !== suggestionsBox) {
        suggestionsBox.innerHTML = "";
        suggestionsBox.classList.remove("active");
      }
    });
  }

  // Multi-item toggles and calculation events
  const itemRows = document.querySelectorAll(".debt-item-row");
  itemRows.forEach(row => {
    const select = row.querySelector(".item-select");
    const customText = row.querySelector(".item-custom-text");
    const priceInp = row.querySelector(".item-price");
    const qtyInp = row.querySelector(".item-qty");

    select.addEventListener("change", (e) => {
      const isOther = e.target.value === "Other";
      customText.style.display = isOther ? "block" : "none";
      if (isOther) {
        customText.setAttribute("required", "true");
      } else {
        customText.removeAttribute("required");
        customText.value = "";
      }

      const hasItem = e.target.value !== "";
      if (hasItem) {
        priceInp.setAttribute("required", "true");
        qtyInp.setAttribute("required", "true");
        if (!qtyInp.value) qtyInp.value = "1";
      } else {
        priceInp.removeAttribute("required");
        qtyInp.removeAttribute("required");
        priceInp.value = "";
        qtyInp.value = "";
      }
      calculateFormTotal();
      updateDebtRowsVisibility();
    });

    customText.addEventListener("input", () => {
      calculateFormTotal();
      updateDebtRowsVisibility();
    });

    priceInp.addEventListener("input", () => {
      calculateFormTotal();
      updateDebtRowsVisibility();
    });

    qtyInp.addEventListener("input", () => {
      calculateFormTotal();
      updateDebtRowsVisibility();
    });
  });

  // Debtor Detail Modal inner actions hooks
  document.getElementById("detail-action-add-debt").addEventListener("click", (e) => {
    closeModal("modal-debtor-detail");
    openAddDebtModal(e.currentTarget.dataset.debtorId);
  });

  document.getElementById("detail-action-pay-total").addEventListener("click", (e) => {
    openAggregatePaymentModal(e.currentTarget.dataset.debtorId);
  });

  document.getElementById("form-aggregate-payment").addEventListener("submit", saveAggregatePayment);

  document.getElementById("detail-action-share").addEventListener("click", (e) => {
    shareDebtorStatement(e.currentTarget.dataset.debtorId);
  });

  document.getElementById("detail-action-print").addEventListener("click", (e) => {
    printDebtorStatement(e.currentTarget.dataset.debtorId);
  });

  document.getElementById("detail-action-edit-profile").addEventListener("click", (e) => {
    const debtorId = e.currentTarget.dataset.debtorId;
    closeModal("modal-debtor-detail");
    
    // Open debtor edit modal
    const debtor = appState.debtors.find(d => d.id === debtorId);
    if (debtor) {
      document.getElementById("debtor-form-id").value = debtor.id;
      document.getElementById("debtor-name").value = debtor.name;
      document.getElementById("debtor-phone").value = debtor.phone || "";
      document.getElementById("debtor-address").value = debtor.address || "";
      document.getElementById("modal-debtor-title").textContent = i18n[appState.currentLang].action_edit;
      openModal("modal-debtor");
    }
  });

  // Sub-tabs on Debtor details profile
  document.querySelectorAll(".detail-tab").forEach(tab => {
    tab.addEventListener("click", (e) => {
      document.querySelectorAll(".detail-tab").forEach(t => t.classList.remove("active"));
      tab.classList.add("active");
      
      const target = tab.dataset.tab; // debts, history
      document.getElementById("detail-tab-debts").classList.toggle("active", target === "debts");
      document.getElementById("detail-tab-history").classList.toggle("active", target === "history");
    });
  });

  // DB Backup Operations
  document.getElementById("btn-export-db").addEventListener("click", exportDatabase);
  
  const importTrigger = document.getElementById("btn-import-trigger");
  const fileInput = document.getElementById("db-import-file");
  importTrigger.addEventListener("click", () => fileInput.click());
  fileInput.addEventListener("change", importDatabase);

  const resetBtn = document.getElementById("btn-reset-db");
  if (resetBtn) {
    resetBtn.addEventListener("click", resetDatabase);
  }

  document.getElementById("form-login").addEventListener("submit", (e) => {
    e.preventDefault();
    const p = document.getElementById("login-password").value.trim();
    const auth = getAuthSettings();
    if (p === auth.password) {
      loginUserSuccess();
    } else {
      const errMsg = document.getElementById("login-error-msg");
      errMsg.style.display = "block";
      errMsg.textContent = appState.currentLang === "km" ? "លេខកូដសម្ងាត់មិនត្រឹមត្រូវ!" : "Invalid password!";
    }
  });

  document.getElementById("form-change-auth").addEventListener("submit", (e) => {
    e.preventDefault();
    const newU = document.getElementById("change-username").value.trim();
    const newP = document.getElementById("change-password").value.trim();
    if (newU && newP) {
      saveAuthSettings(newU, newP);
      const msgSpan = document.getElementById("change-auth-msg");
      if (msgSpan) {
        msgSpan.style.color = "var(--success)";
        msgSpan.textContent = appState.currentLang === "km" ? "រក្សាទុកការផ្លាស់ប្តូរជោគជ័យ!" : "Account credentials updated!";
      }
      showToast(appState.currentLang === "km" ? "គណនីគ្រប់គ្រងត្រូវបានកែប្រែដោយជោគជ័យ!" : "Account settings updated successfully!");
      resetInactivityTimer();
    }
  });

  const oldLogoutBtn = document.getElementById("btn-logout");
  if (oldLogoutBtn) {
    oldLogoutBtn.addEventListener("click", () => {
      logoutUser(true);
    });
  }
  
  const logoutSettingsBtn = document.getElementById("btn-logout-settings");
  if (logoutSettingsBtn) {
    logoutSettingsBtn.addEventListener("click", () => {
      logoutUser(true);
    });
  }
}

// BACKUP & RESTORE IMPLEMENTATIONS
function exportDatabase() {
  const dataStr = "data:text/json;charset=utf-8," + encodeURIComponent(JSON.stringify(appState));
  const dlAnchorElem = document.createElement("a");
  
  const formattedDate = new Date().toISOString().slice(0,10);
  
  dlAnchorElem.setAttribute("href", dataStr);
  dlAnchorElem.setAttribute("download", `debt_manager_backup_${formattedDate}.json`);
  dlAnchorElem.click();
}

function importDatabase(e) {
  const fileReader = new FileReader();
  fileReader.onload = function(event) {
    try {
      const importedState = JSON.parse(event.target.result);
      
      if (importedState.debtors && importedState.debts && importedState.payments) {
        appState.debtors = importedState.debtors;
        appState.debts = importedState.debts;
        appState.payments = importedState.payments;
        
        saveToLocalStorage();
        showToast(i18n[appState.currentLang].toast_imported);
        renderCurrentView();
      } else {
        alert("Invalid backup file structure!");
      }
    } catch (err) {
      alert("Error parsing backup JSON file!");
    }
  };
  fileReader.readAsText(e.target.files[0]);
}

function resetDatabase() {
  if (confirm(i18n[appState.currentLang].confirm_reset)) {
    localStorage.clear();
    appState.debtors = [];
    appState.debts = [];
    appState.payments = [];
    
    // Fallback reload defaults
    initDatabase();
    showToast(i18n[appState.currentLang].toast_reset_success);
    renderCurrentView();
  }
}

// UI GENERAL HELPERS
function getTodayDateString() {
  const today = new Date();
  const yyyy = today.getFullYear();
  let mm = today.getMonth() + 1; // Months start at 0
  let dd = today.getDate();

  if (dd < 10) dd = "0" + dd;
  if (mm < 10) mm = "0" + mm;

  return `${yyyy}-${mm}-${dd}`;
}

function getCurrentTimeFormatted(lang) {
  const now = new Date();
  const hours = now.getHours();
  const minutes = now.getMinutes().toString().padStart(2, "0");
  
  if (lang === "km") {
    const period = hours >= 12 ? (hours >= 17 ? "ល្ងាច" : "រសៀល") : "ព្រឹក";
    const displayHours = hours % 12 || 12;
    return `ម៉ោង ${displayHours}:${minutes} ${period}`;
  } else {
    const period = hours >= 12 ? "PM" : "AM";
    const displayHours = hours % 12 || 12;
    return `${displayHours}:${minutes} ${period}`;
  }
}

function formatCurrency(amount, currency) {
  if (currency === "USD") {
    return "$" + amount.toFixed(2);
  } else {
    return Math.round(amount).toLocaleString() + " ៛";
  }
}

function showToast(message) {
  const toast = document.getElementById("toast-notification");
  const toastMsg = document.getElementById("toast-message");
  
  if (toast && toastMsg) {
    toastMsg.textContent = message;
    toast.classList.add("active");
    
    setTimeout(() => {
      toast.classList.remove("active");
    }, 3000);
  }
}

// Toggle password visibility on Login screen
function togglePassword() {
  const passwordInput = document.getElementById("login-password");
  const eyeIcon = document.getElementById("eye-icon");
  if (!passwordInput || !eyeIcon) return;
  
  if (passwordInput.type === "password") {
    passwordInput.type = "text";
    eyeIcon.innerHTML = `
      <path stroke-linecap="round" stroke-linejoin="round" d="M3.98 8.223A10.477 10.477 0 0 0 1.934 12C3.226 16.338 7.244 19.5 12 19.5c.993 0 1.953-.138 2.863-.395M6.228 6.228A10.451 10.451 0 0 1 12 4.5c4.756 0 8.773 3.162 10.065 7.498a10.522 10.522 0 0 1-4.293 5.774M6.228 6.228 3 3m3.228 3.228 3.65 3.65m7.894 7.894L21 21m-3.228-3.228-3.65-3.65m0 0a3 3 0 1 0-4.243-4.243m4.242 4.242L9.88 9.88" />
    `;
  } else {
    passwordInput.type = "password";
    eyeIcon.innerHTML = `
      <path stroke-linecap="round" stroke-linejoin="round" d="M2.036 12.322a1.012 1.012 0 0 1 0-.639C3.423 7.51 7.36 4.5 12 4.5c4.638 0 8.573 3.007 9.963 7.178.07.207.07.43 0 .639C20.577 16.49 16.64 19.5 12 19.5c-4.638 0-8.573-3.007-9.963-7.178Z" />
      <path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 1 1-6 0 3 3 0 0 1 6 0Z" />
    `;
  }
}

// ==========================================
// CLOUD SYNCHRONIZE ENGINE (FIREBASE V8)
// ==========================================

let firebaseDbRef = null;
let firebaseConnectedListener = null;
let isWritingToCloud = false;

function initCloudSync() {
  const syncEnabled = localStorage.getItem(STORAGE_KEYS.SYNC_ENABLED) === "true";
  const syncConfigStr = localStorage.getItem(STORAGE_KEYS.SYNC_CONFIG) || "";
  const syncShopId = localStorage.getItem(STORAGE_KEYS.SYNC_SHOP_ID) || "sok_sinet_shop";
  
  const form = document.getElementById("form-sync-config");
  const configTextarea = document.getElementById("sync-json-config");
  const shopIdInput = document.getElementById("sync-shop-id");
  
  if (configTextarea) configTextarea.value = syncConfigStr;
  if (shopIdInput) shopIdInput.value = syncShopId;
  
  if (syncEnabled) {
    if (form) form.style.display = "flex";
    if (syncConfigStr) {
      try {
        const config = JSON.parse(syncConfigStr);
        connectFirebase(config, syncShopId);
      } catch (e) {
        updateSyncStatus("error");
      }
    } else {
      updateSyncStatus("connecting");
    }
  } else {
    if (form) form.style.display = "none";
    updateSyncStatus("disabled");
  }
  
  // Event listeners
  document.getElementById("btn-toggle-sync").addEventListener("click", () => {
    const isCurrentlyEnabled = localStorage.getItem(STORAGE_KEYS.SYNC_ENABLED) === "true";
    const lang = appState.currentLang;
    
    if (isCurrentlyEnabled) {
      localStorage.setItem(STORAGE_KEYS.SYNC_ENABLED, "false");
      if (form) form.style.display = "none";
      disconnectFirebase();
      showToast(i18n[lang].toast_sync_disabled);
    } else {
      localStorage.setItem(STORAGE_KEYS.SYNC_ENABLED, "true");
      if (form) form.style.display = "flex";
      updateSyncStatus("connecting");
      showToast(i18n[lang].toast_sync_enabled);
      
      const configStr = localStorage.getItem(STORAGE_KEYS.SYNC_CONFIG);
      if (configStr) {
        try {
          const config = JSON.parse(configStr);
          connectFirebase(config, localStorage.getItem(STORAGE_KEYS.SYNC_SHOP_ID) || "sok_sinet_shop");
        } catch (e) {
          updateSyncStatus("error");
        }
      }
    }
  });
  
  document.getElementById("form-sync-config").addEventListener("submit", (e) => {
    e.preventDefault();
    const lang = appState.currentLang;
    const rawConfig = document.getElementById("sync-json-config").value.trim();
    const shopId = document.getElementById("sync-shop-id").value.trim() || "sok_sinet_shop";
    
    let parsedConfig = null;
    
    try {
      let jsonString = rawConfig;
      if (jsonString.includes("{") && jsonString.includes("}")) {
        const start = jsonString.indexOf("{");
        const end = jsonString.lastIndexOf("}");
        jsonString = jsonString.substring(start, end + 1);
      }
      
      // Clean up Javascript object literal notation to standard JSON
      let cleanedJson = jsonString
        .replace(/([{,]\s*)([a-zA-Z0-9_]+)\s*:/g, '$1"$2":')
        .replace(/'/g, '"')
        .replace(/,\s*([\]}])/g, '$1');
        
      parsedConfig = JSON.parse(cleanedJson);
    } catch (err) {
      console.error("Config parse error:", err);
      alert(lang === "km" ? "កូដ Firebase Config មិនត្រឹមត្រូវ! សូមប្រាកដថាអ្នកបានចម្លង JSON object ត្រឹមត្រូវ។" : "Invalid Firebase Config! Please make sure it is a valid JSON object.");
      return;
    }
    
    if (!parsedConfig || !parsedConfig.databaseURL) {
      alert(lang === "km" ? "រកមិនឃើញ databaseURL ទេ! Firebase Realtime Database ត្រូវការ databaseURL ដើម្បីដំណើរការ។" : "Missing databaseURL! Firebase Realtime Database needs a databaseURL to operate.");
      return;
    }
    
    localStorage.setItem(STORAGE_KEYS.SYNC_CONFIG, JSON.stringify(parsedConfig, null, 2));
    localStorage.setItem(STORAGE_KEYS.SYNC_SHOP_ID, shopId);
    localStorage.setItem(STORAGE_KEYS.SYNC_ENABLED, "true");
    
    showToast(i18n[lang].toast_sync_saved);
    connectFirebase(parsedConfig, shopId);
  });
  
  document.getElementById("btn-push-to-cloud").addEventListener("click", pushLocalDataToCloud);
}

function disconnectFirebase() {
  if (firebaseDbRef) {
    firebaseDbRef.off();
    firebaseDbRef = null;
  }
  if (firebaseConnectedListener) {
    try {
      firebase.database().ref(".info/connected").off("value", firebaseConnectedListener);
    } catch (e) {}
    firebaseConnectedListener = null;
  }
  try {
    if (firebase.apps.length > 0) {
      firebase.app().delete();
    }
  } catch (e) {
    console.error("Error deleting Firebase app:", e);
  }
  updateSyncStatus("disabled");
}

function connectFirebase(config, shopId) {
  disconnectFirebase();
  updateSyncStatus("connecting");
  
  try {
    if (firebase.apps.length === 0) {
      firebase.initializeApp(config);
    }
    const db = firebase.database();
    
    firebaseConnectedListener = db.ref(".info/connected").on("value", (snap) => {
      if (snap.val() === true) {
        updateSyncStatus("connected");
        startListeningToCloud(db, shopId);
      } else {
        updateSyncStatus("connecting");
      }
    }, (err) => {
      console.error("Connection state listener error:", err);
      updateSyncStatus("error");
    });
    
  } catch (err) {
    console.error("Firebase init error:", err);
    updateSyncStatus("error");
    showToast(appState.currentLang === "km" ? "ការភ្ជាប់ Firebase បានបរាជ័យ!" : "Firebase initialization failed!");
  }
}

function startListeningToCloud(db, shopId) {
  const path = `shops/${shopId}`;
  firebaseDbRef = db.ref(path);
  
  firebaseDbRef.on("value", (snapshot) => {
    if (isWritingToCloud) {
      isWritingToCloud = false;
      return;
    }
    
    const data = snapshot.val();
    if (data && (data.debtors || data.debts || data.payments)) {
      console.log("Syncing database from Cloud:", data);
      
      const localDataStr = JSON.stringify({
        debtors: appState.debtors,
        debts: appState.debts,
        payments: appState.payments
      });
      const cloudDataStr = JSON.stringify({
        debtors: data.debtors || [],
        debts: data.debts || [],
        payments: data.payments || []
      });
      
      if (localDataStr !== cloudDataStr) {
        appState.debtors = data.debtors || [];
        appState.debts = data.debts || [];
        appState.payments = data.payments || [];
        
        localStorage.setItem(STORAGE_KEYS.DEBTORS, JSON.stringify(appState.debtors));
        localStorage.setItem(STORAGE_KEYS.DEBTS, JSON.stringify(appState.debts));
        localStorage.setItem(STORAGE_KEYS.PAYMENTS, JSON.stringify(appState.payments));
        
        const activeModals = document.querySelectorAll(".modal-overlay.active");
        const activeElement = document.activeElement;
        const isTyping = activeElement && (activeElement.tagName === "INPUT" || activeElement.tagName === "TEXTAREA" || activeElement.tagName === "SELECT");
        
        if (activeModals.length === 0 && !isTyping) {
          renderCurrentView();
        }
      }
    } else {
      console.log("Cloud database path is empty or uninitialized.");
    }
  }, (err) => {
    console.error("Cloud database listener error (check security rules):", err);
    updateSyncStatus("error");
  });
}

function syncDataToCloud() {
  const syncEnabled = localStorage.getItem(STORAGE_KEYS.SYNC_ENABLED) === "true";
  if (syncEnabled && firebaseDbRef) {
    isWritingToCloud = true;
    firebaseDbRef.set({
      debtors: appState.debtors,
      debts: appState.debts,
      payments: appState.payments,
      lastUpdated: Date.now()
    }).catch(err => {
      console.error("Sync data to cloud failed:", err);
      isWritingToCloud = false;
    });
  }
}

function pushLocalDataToCloud() {
  const lang = appState.currentLang;
  const msg = i18n[lang].confirm_push_cloud || "Are you sure you want to push local data to Cloud? This will overwrite Cloud data.";
  if (!confirm(msg)) return;
  
  if (firebaseDbRef) {
    updateSyncStatus("connecting");
    firebaseDbRef.set({
      debtors: appState.debtors,
      debts: appState.debts,
      payments: appState.payments,
      lastUpdated: Date.now()
    }).then(() => {
      updateSyncStatus("connected");
      showToast(i18n[lang].toast_push_success);
    }).catch(err => {
      console.error("Push data to cloud failed:", err);
      updateSyncStatus("error");
      showToast(lang === "km" ? "ការរុញទិន្នន័យបានបរាជ័យ!" : "Failed to push data!");
    });
  } else {
    showToast(lang === "km" ? "សូមភ្ជាប់ទៅកាន់ Database ជាមុនសិន!" : "Please connect to Database first!");
  }
}

function updateSyncStatus(status) {
  const dot = document.getElementById("sync-status-dot");
  const text = document.getElementById("sync-status-text");
  const btnToggle = document.getElementById("btn-toggle-sync");
  const lang = appState.currentLang;
  
  if (!dot || !text || !btnToggle) return;
  
  dot.className = "";
  dot.style.backgroundColor = "";
  
  if (status === "connected") {
    dot.classList.add("sync-status-dot-connected");
    text.textContent = i18n[lang].sync_status_connected || "Connected (Realtime)";
    btnToggle.textContent = i18n[lang].btn_sync_disable || "Disable Sync";
    btnToggle.className = "btn btn-danger btn-sm";
  } else if (status === "connecting") {
    dot.classList.add("sync-status-dot-connecting");
    text.textContent = i18n[lang].sync_status_connecting || "Connecting...";
    btnToggle.textContent = i18n[lang].btn_sync_disable || "Disable Sync";
    btnToggle.className = "btn btn-danger btn-sm";
  } else if (status === "error") {
    dot.classList.add("sync-status-dot-error");
    text.textContent = i18n[lang].sync_status_error || "Connection Error";
    btnToggle.textContent = i18n[lang].btn_sync_disable || "Disable Sync";
    btnToggle.className = "btn btn-danger btn-sm";
  } else {
    dot.classList.add("sync-status-dot-disabled");
    text.textContent = i18n[lang].sync_status_disabled || "Disconnected";
    btnToggle.textContent = i18n[lang].btn_sync_enable || "Enable Sync";
    btnToggle.className = "btn btn-outline btn-sm";
  }
}
